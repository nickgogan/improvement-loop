---
name: "Implement /identify-artifacts theme-graduation detection (DD-99) + DD-94 enum bullet add for `theme-graduation`"
id: "IB-160"
source_dd: "DD-99"
status: "Queued"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Phase-3 lifecycle bundle (alongside IB-159, IB-161, IB-162, IB-163, IB-164). Implements DD-99's graduation-trigger detection at the natural read point per DD-99 §Detection: `/identify-artifacts` Step 6 routing-table read for unrouted-bucket review. **Scope.** Two work items, both required for DD-99 to be operative on the next identification cycle. **Item 1 — `/identify-artifacts` detection.** Add detection logic to Step 6 (Unrouted Bucket review): for the bucket's current contents, evaluate the trigger conjunction — (a) finding-count threshold (≥5 unrouted findings cluster by category, tag, or `same-problem` link) AND (b) linkage threshold (the cluster shares `same-problem` relationships per the four-relationship type system codified by `/finding-crosslink`; at least one `same-problem` link spans the cluster — categorical co-occurrence alone does NOT trigger). On match, flag the cluster in the identification report's 'Candidate Cluster' section per spec §2.4 AND emit a structured graduation proposal at `operations/graduation-proposals/<YYYY-MM-DD>-<theme>-graduation-proposal.md` per DD-99 §Response shape — theme identity (proposed name, current Unrouted Bucket entries, total finding count), linkage map (`same-problem` relationships among findings; cross-category and cross-dimension links called out explicitly), path recommendation from closed enum (PROMOTE — new dimension + guide cluster | ABSORB — into existing dimension | DEFER — sub-threshold linkage), per-path execution sketch (artifacts the executing path would touch: research-dimensions.md, routing-table rows, new-guide stub, dimension-rebalance scope), cross-dimension flag (set when findings span ≥2 existing dimensions). **Cross-dimension findings disqualify ABSORB** — when the flag is set, the recommendation MUST be PROMOTE or DEFER; ABSORB-with-cross-dimension is invalid by contract. Read-only by contract: never edit research-dimensions.md, never update routing table, never create new guide stubs, never invoke `/dimension-rebalance` based on its own proposal. One proposal carries exactly one path recommendation; ambiguous proposals (PROMOTE-vs-ABSORB undecided) are a defect — DEFER with explanation is the resolution. Stale-proposal hygiene: if between emission and Nick's ruling the cluster's finding count drops below 5, Codifier flags the proposal as stale on next detection cycle (append 'stale-as-of-<date>' header); the stale proposal is retained for audit, not deleted; a new proposal supersedes if the cluster re-crosses threshold later. **Item 2 — DD-94 enum bullet edit.** Add `theme-graduation` as a closed-enum trigger tag in DD-94's §The Constraint enum bullet list (current enum after IB-155: `staleness-threshold` | `nick-request` | `dimension-rebalance` | `finding-removed` | `structural-edit`; IB-159 also adds `guide-split` independently). The `theme-graduation` tag is used in EXACTLY one context per DD-99 §Trigger-Tag Enum Amendment: the **PROMOTE path only**, on the new guide's first changelog entry. Body line: 'First synthesis from graduated theme [[<theme-name>]] per [[DD-XXX]]' where DD-XXX is the per-graduation DD. The ABSORB path uses the existing `dimension-rebalance` tag — no amendment needed for that path. The `/synthesize-guide` Step 4.5 appender's enum check (added in IB-155) must be updated to accept `theme-graduation` in addition to the current 5 tags (and IB-159's `guide-split` if both have landed). Cross-IB note: IB-159 also adds an enum entry (`guide-split`); the two edits are additive and non-conflicting — each IB owns its own bullet addition independently. **Rules (subset of DD-99 §Rules for Codifier).** (1) Detect on `/identify-artifacts` Step 6 read of the routing table; flag in identification report AND emit graduation proposal as separate artifact (same separation pattern as DD-96 drift reports and DD-98 split proposals). (2) Both thresholds required; categorical co-occurrence without `same-problem` link is NOT a trigger. (3) One proposal, one recommendation — PROMOTE | ABSORB | DEFER; ambiguity defaults to DEFER with rationale. (4) Cross-dimension findings disqualify ABSORB; recommendation must be PROMOTE or DEFER. (5) Never auto-execute either path; PROMOTE requires per-graduation DD; ABSORB requires Nick's ruling on the proposal + routing-table update + `/dimension-rebalance` invocation. (6) Stale-proposal hygiene with append-not-delete semantics. **Acceptance Criteria (subset of DD-99 §Acceptance Criteria).** (i) Unrouted Bucket holds 4 findings with `same-problem` links — NO proposal emitted (sub-threshold count); (ii) Unrouted Bucket holds 5 findings with NO `same-problem` links — NO proposal emitted (linkage threshold not met); (iii) Unrouted Bucket holds 5 findings with `same-problem` links, all in one existing dimension — proposal emitted; recommendation may be ABSORB or PROMOTE based on Codifier's structural-distinctness read; (iv) Unrouted Bucket holds 5 findings with `same-problem` links spanning 2 existing dimensions — proposal emitted; recommendation MUST be PROMOTE or DEFER; ABSORB is invalid; (v) Codifier auto-creates a new dimension based on its own proposal — procedural violation, flagged in next governance audit; (vi) Cluster crosses threshold; proposal emitted; one finding gets reclassified out before Nick gates — Codifier flags proposal as stale on next detection cycle; proposal retained with stale header; (vii) After IB-160 lands, `/synthesize-guide` Step 4.5 appender accepts `theme-graduation` as valid trigger tag; (viii) DD-94's §The Constraint enum bullet list shows `theme-graduation` as a valid entry. **Dependencies.** IB-155 (DD-94 enum infrastructure exists in the appender to extend); `/finding-crosslink` skill produces the `same-problem` link relations the trigger evaluates against (already in production per DD-82 architecture). No blocking dependency on IB-159 (each IB executes its own DD-94 bullet edit independently); IB-153 is referenced by DD-99's ABSORB execution path but is not a build-time dependency for IB-160 (the graduation proposal flags ABSORB recommendation; `/dimension-rebalance` is the downstream executor when Nick rules ABSORB). **Touch Points.** `.claude/skills/identify-artifacts/SKILL.md` (Step 6 detection insert; coordinate with IB-159 if both touch the same step in the same session); `project-management/design-decisions/DD-94.md` (single bullet add to §The Constraint enum list); `.claude/skills/synthesize-guide/SKILL.md` (Step 4.5 enum extension to accept `theme-graduation`); new directory `operations/graduation-proposals/` created on first proposal emission (not pre-created). **Form precedent.** IB-157 (new operations directory + structured report shape); IB-155 (multi-work-item structure)."
milestone: null
---
