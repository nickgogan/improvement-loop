---
name: "Implement /identify-artifacts theme-graduation detection (DD-99) + DD-94 enum bullet add for `theme-graduation`"
id: "IB-160"
source_dd:
  - "DD-99"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 76. **Item 1 — `/identify-artifacts` detection (Step 6.b).** Inserted Step 6.b (Theme-Graduation Detection) inline within Step 6 (Guide Cluster Check), AFTER IB-159's Step 6.a (Split-Trigger Detection). Step 6.b evaluates the DD-99 trigger against the Unrouted Bucket: conjunction (count ≥5 AND ≥1 `same-problem` link spans the cluster). Cross-dimension flag set when findings span ≥2 existing dimensions per the Dimension → Guide mapping; cross-dimension findings DISQUALIFY ABSORB (recommendation MUST be PROMOTE or DEFER per DD-99 §The Constraint). On both-thresholds match: (a) flags cluster in identification report's Candidate Cluster section per spec §2.4 with DD-99 marker; (b) emits structured graduation-proposal artifact at `operations/graduation-proposals/<YYYY-MM-DD>-<theme>-graduation-proposal.md` with full per-DD-99-§Response shape (theme identity; linkage map of `same-problem` relationships; cross-dimension flag; PROMOTE/ABSORB/DEFER recommendation from closed enum; per-path execution sketch listing artifacts the executing path would touch); (c) summary block heads-up. Outcome dispatch table covers four cases: both-thresholds (proposal); count-only (no proposal; inline note 'no same-problem linkage detected'); linkage-only (no proposal; inline 'below volume threshold'); neither (no-op). Stale-proposal hygiene: append `## Stale as of <date>` header below title heading on next detection cycle if cluster drops below 5; proposal RETAINED for audit (not deleted); new proposal emitted if cluster re-crosses threshold later. One proposal, one recommendation invariant per DD-99 §Rules #3; ambiguity defaults to DEFER with explanation. Read-only by contract: never edits research-dimensions.md, never updates routing table, never creates new guide stubs, never invokes `/dimension-rebalance` based on its own proposal. Coordinated-edit posture: Step 6.b is additive to Step 6.a (IB-159) — the two work items targets different routing-table surfaces (Step 6.a = Active Clusters per-cluster trigger; Step 6.b = Unrouted Bucket cluster review); no edit collision. **Item 2 — DD-94 enum bullet add (`theme-graduation`).** Edited DD-94 §The Constraint enum bullet list to add the `theme-graduation` bullet AFTER the `guide-split` bullet (which IB-159 added). Bullet body documents the single-context use per DD-99 §Trigger-Tag Enum Amendment: PROMOTE path ONLY, new guide's first changelog entry. Body line: 'First synthesis from graduated theme [[<theme-name>]] per [[DD-XXX]]' where DD-XXX is the per-graduation DD. ABSORB path uses existing `dimension-rebalance` tag — no amendment for ABSORB. Cites DD-99 (Phase-3 ratification, session 74) and IB-160 (implementation, session 76). `/synthesize-guide` Step 4.5 appender's enum check updated to accept `theme-graduation` in addition to the prior 6 tags (closed enum now: `staleness-threshold`, `nick-request`, `dimension-rebalance`, `finding-removed`, `structural-edit`, `guide-split`, `theme-graduation`). **DD-table additions.** `/identify-artifacts` DD table gains DD-99 (full row citing Step 6.b + closed-enum recommendations + cross-dimension constraint + stale-proposal hygiene + read-only-by-contract + DD-94 `theme-graduation` linkage). `/synthesize-guide` DD table gains DD-99 (citing PROMOTE-path executor role + `theme-graduation` enum acceptance at Step 4.5; ABSORB path delegated to `/dimension-rebalance` + destination guide's next regen using existing `dimension-rebalance` tag). **Failure-mode additions to `/identify-artifacts`.** Six new rows folded into the failure-modes table (these were not added in IB-159's commit because that commit focused on synthesize-guide; folding both IB-159 and IB-160 identify-artifacts failure-modes here for table consistency): (i) DD-98 split single-condition observation; (ii) DD-99 graduation single-condition observation; (iii) cross-dimension graduation cluster + Codifier proposes ABSORB (invalid by contract); (iv) stale graduation-proposal cycle (cluster drops below 5; append stale header); (v) split-/graduation-proposal filename collision (same theme/guide, same day; append `-2`/`-3`). Note: rows (i) and (v)'s split-side coverage attributable to IB-159 retroactively; documented in this IB closure. **Acceptance addressed.** (i) Unrouted Bucket holds 4 findings with `same-problem` links → NO proposal (sub-threshold count) — Step 6.b dispatch table linkage-only path covers this when count<5. (ii) Unrouted Bucket holds 5 findings with NO `same-problem` links → NO proposal (linkage threshold not met) — count-only dispatch path. (iii) Unrouted Bucket holds 5 findings with `same-problem` links, all in one existing dimension → proposal emitted; recommendation may be ABSORB or PROMOTE based on Codifier's structural-distinctness read — proposal-shape covers this with `recommendation` field + `cross_dimension: false` flag. (iv) Unrouted Bucket holds 5 findings with `same-problem` links spanning 2 existing dimensions → proposal emitted with `cross_dimension: true`; recommendation MUST be PROMOTE or DEFER (ABSORB invalid) — failure-modes row + DD table row enforce this. (v) Codifier auto-creates new dimension based on its own proposal → procedural violation: read-only-by-contract invariant; flagged in next governance audit. (vi) Cluster crosses threshold; proposal emitted; one finding gets reclassified out before Nick gates → Codifier flags proposal as stale on next detection cycle; proposal retained with stale header — failure-modes row covers this with the append-not-delete semantics. (vii) After IB-160 lands, Step 4.5 appender accepts `theme-graduation` as valid trigger tag — confirmed in enum-validation line of synthesize-guide. (viii) DD-94's §The Constraint enum bullet list shows `theme-graduation` — confirmed in DD-94 edit. **Executor's-choice resolutions.** (1) Step number for identify-artifacts graduation detection: Step 6.b (additive within Step 6's existing routing-table-read flow; sibling to Step 6.a from IB-159; covers Unrouted Bucket while Step 6.a covers Active Clusters; clean separation). (2) DD-94 enum bullet placement: AFTER `guide-split` (newest tags accumulate at the bottom of the bullet list; matches the chronological ordering of additions). (3) Failure-mode-table consolidation: folded both IB-159 and IB-160 identify-artifacts failure-mode rows into this IB-160 commit for table consistency; documented retroactively. (4) `theme-graduation` enum extension on Step 4.5 of `/synthesize-guide`: landed additively in this IB-160 commit (separate from IB-159's `guide-split` extension). **Bugs surfaced.** None. **Contract amendments proposed/applied.** Edit to DD-94 body is not an amendment per DD-44 — DD-99 IS the DD amendment for the new tag (per DD-99 §Trigger-Tag Enum Amendment to DD-94); IB-160 is the mechanical execution of DD-99's amendment via the bullet edit. No DD frontmatter `updated:` field touched on DD-94 (additive enum-bullet edit only). **Logged for future.** (a) The PROMOTE/ABSORB/DEFER closed-enum recommendation field will accumulate empirical signal as the corpus exercises the path. If recommendation drift surfaces (Nick frequently overrides Codifier's read), tighten the closed enum or add a structured rubric for cross-dimension structural-distinctness judgment. Defer until observed. (b) Step 6.b's stale-proposal hygiene is append-not-delete; if `operations/graduation-proposals/` accumulates many stale proposals over time (>5 per active theme), a future archive policy may be useful. Parallel to DD-100's pruning-threshold open question. (c) Step 6.b currently writes the proposal artifact unconditionally on both-thresholds match; if Nick routinely accepts ABSORB without DD weight, the asymmetric ruling-on-proposal-direct path (DD-99 §Why) may be informally observable as ABSORB-rate-vs-PROMOTE-rate. Surface metrics if/when a third graduation proposal is emitted. (d) ABSORB-path execution flow (proposal → Nick rules → routing-table update + `/dimension-rebalance` invocation) is currently manual — Codifier writes nothing post-ruling. If ABSORB volume justifies it, a follow-up IB may promote the post-ruling steps to a skill mode. (e) DD-94 enum is now 7 entries deep (5 original + `guide-split` + `theme-graduation`). The closed-vocabulary cost is acceptable; revisit if Phase-N adds further entries. (f) The seven-entry enum's display in run-report rejection messages (when an invalid tag is passed) should remain readable — verify wording on first real rejection encounter."
milestone: null
---
