---
name: "Build /detect-drift skill — non-guide source drift scanner (DD-96)"
id: "IB-157"
source_dd: "DD-96"
status: "Open"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Codifier scope. Build a new on-demand skill at `.claude/skills/detect-drift/SKILL.md`. Procedure: enumerate `extracts/{rules,skills,templates,agents}/*.md` (skip `_index.md`); for each artifact, read `source_finding` from frontmatter, resolve to `research-findings/<slug>.md`; compare `source_finding.updated` against `artifact.extraction_date`; emit drift entry per DD-96 §The Constraint shape if `source.updated > artifact.extraction_date` (strict — equal dates are NOT drift); write report at `operations/drift-reports/<YYYY-MM-DD>-source-drift.md` with one-paragraph summary (total scanned, drift hits, clean count, invocation context) followed by per-entry blocks. Read-only invariant: skill must not modify any artifact's frontmatter, body, or `pipeline_status`; must not invoke `/extract-artifacts` or `/identify-artifacts`. Recommendation enum is closed three-value (`re-run /extract-artifacts on this finding` | `dismiss as cosmetic` | `reclassify`); skill emits Codifier's recommendation but Nick rules on the report. Acceptance: 6 cases from DD-96 §Acceptance Criteria — drift case detected; clean corpus produces summary-only report; equal dates not flagged; modification-attempt fails; guides excluded from scan; procedural-violation case (Codifier auto-re-extracting without Nick ruling) flagged in governance audit. Phase-2 lifecycle bundle (alongside IB-158). New directory `operations/drift-reports/` will be created on first scan run; IB does not pre-create."
milestone: null
---
