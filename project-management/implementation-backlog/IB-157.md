---
name: "Build /detect-drift skill — non-guide source drift scanner (DD-96)"
id: "IB-157"
source_dd: "DD-96"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 71 (Phase B scope expansion). Created new skill at `.claude/skills/detect-drift/SKILL.md` (~250 lines). Read-only on-demand source-drift scanner. Allowed-tools: Read Grep Glob Write (no Edit, no Agent). Procedure: Step 0 resolves scan scope (`--include`/`--exclude` mutually exclusive form filters; `--context` invocation-context tag); Step 1 enumerates `extracts/{rules,skills,templates,agents}/` (skips `_index.md`), parses frontmatter for `source_finding` + `extraction_date` + `assigned_form`, logs missing-frontmatter artifacts under `enumeration_gaps`; Step 2 reads each source finding, captures `last_updated`, compares strict-greater-than (`source.last_updated > artifact.extraction_date`; equal dates NOT drift), logs unresolvable-source / missing-last-updated / malformed-date cases under `unresolvable_sources`; Step 3 emits Codifier recommendation per drift hit from the closed three-value enum (`re-run /extract-artifacts on this finding` | `dismiss as cosmetic` | `reclassify`) — defaults to re-run-suggestion on ambiguous judgment with one-line note; Step 4 constructs report at `operations/drift-reports/<YYYY-MM-DD>-source-drift.md` with frontmatter (counters, forms_scanned, invocation_context) + scan-summary paragraph + Enumeration Gaps section (omitted if zero) + Unresolvable Sources section (omitted if zero) + Drift Hits section (omitted if zero) + per-hit blocks in artifact-stem alphabetical order; Step 5 writes report (atomic) with hyphen-numeric collision suffix on same-date second-runs and surfaces summary to user with explicit Nick-gate-prompt for re-extraction. Read-only invariant codified in Rules #1–#2 and surfaced as a procedural-failure mode (Step 5 path check); skill never invokes `/extract-artifacts` or `/identify-artifacts`; never modifies artifact frontmatter, body, or `pipeline_status`. Closed Recommendation enum codified in Rules #4 — free-form text in the Recommendation field is a contract violation. Guides + patterns excluded (governed by DD-93/94 and DD-81 respectively). All 6 acceptance cases from DD-96 §Acceptance Criteria addressed by procedure design. Bugs surfaced: DD-96 field-name vs live-schema discrepancy — DD-96 §The Constraint and §Rules #2 specify `source_finding.updated`; findings actually carry `last_updated` (588/588 in current KB; 0 carry `updated`). Resolution: skill reads `last_updated`; semantic intent (most-recent source-content update timestamp) preserved; documented in skill Rules #7. Amendments proposed: DD-96 amendment candidate to read `source_finding.last_updated` instead of `source_finding.updated` — filing deferred per session-71 handoff §Rules (no new DDs / IBs mid-session); surface via `/track dd update` or future Owner governance audit. Logged for future: trigger promotion to periodic (per DD-96 §Why) if scan volume justifies it. Live validation deferred to first scan against the live KB (smoke test for read paths and enumeration-gap counts). `operations/drift-reports/` is created on first run; not pre-created. Phase-2 lifecycle bundle (alongside IB-158)."
milestone: null
---
