---
name: "Backfill existing extracts with DD-92 ContextSpec"
id: "IB-151"
source_dd: "DD-92"
status: "Queued"
target_system: "improvement-loop"
priority: "P2"
type: "Maintenance"
notes: "Retrofit existing extracts in `extracts/{rules,patterns,skills,templates,agents}/` with ContextSpec per DD-92. Non-mechanical work: `applies_to` must be re-derived in universal vocabulary, not copied from source finding `applicability` (which uses MetaSystem scope labels S2/S3/General). Also strip IL classification meta (confidence, tier, reason_codes, co_occurrence) from each retrofit. Scope: ~9 existing extracts in `extracts/rules/`, plus whatever exists in `extracts/{patterns,skills,templates,agents}/` at execution time. Reference implementation: `extracts/rules/confirm-failure-first-tdd.md`. Acceptance: every file in `extracts/` (excluding guides and indexes) carries a ContextSpec block in frontmatter and no IL classification meta. Likely a single-session sweep."
milestone: null
---
