---
name: "/extract-artifacts argument shape mismatch with IB-163 row-heading shape — Step 0a contract bug"
id: "IB-166"
source_dd:
  - "DD-101"
status: "Done"
target_system: "improvement-loop"
priority: "P3"
type: "Fix"
notes: "Discovered + closed session 82 during IB-164's first-execution session-start structural verification pass (the verification specifically flagged in IB-163 closure note (a) as load-bearing at IB-164 execution). **Defect.** `/extract-artifacts` SKILL.md Step 0a step 1 declared the `--harvest-row` / `--harvest-dismiss` argument shape as `<guide-stem>::<target-form>::<headline-slug>` and Step 0a step 3 instructed a literal heading match against `### <guide-stem>::<target-form>::<headline-slug>`. But IB-163 + `/synthesize-guide` Step 4.7 writes per-row block headings as `### <finding-stem>::<target-form>::<headline-slug>` (canonical row-ID per IB-163 closure note (a) + DD-101 §The Constraint). Result: under the IB-164-as-shipped contract, no harvest-row argument with a real `<guide-stem>` prefix could resolve to a real row heading — Step 0a step 3 would always abort. The IB-164 spec was internally consistent (path resolution and heading lookup both used `<guide-stem>` as the first component), but it deviated from the canonical writer-side shape that IB-163 had already established. **Root cause.** IB-164 was specced before IB-164 was executed against any real queue file; the `<guide-stem>` argument-prefix substitution (motivated by per-guide queue file path resolution) was applied without verifying that the row-heading shape on the consumer side matched. IB-163 closure note (a) explicitly logged this risk for verification at IB-164 execution time. The verification window opened at session 82's first invocation. **Closure mechanism (session 82).** Patched `/extract-artifacts/SKILL.md` inline (single same-session amendment, Nick-gated): (1) Arguments table — `--harvest-row` and `--harvest-dismiss` rows updated to declare argument shape `<finding-stem>::<target-form>::<headline-slug>`; queue-file resolution amended to auto-locate by literal heading match across `extracts/guides/*.harvest-queue.md`; defensive multi-match abort path; new optional `--guide <guide-stem>` flag for disambiguation. (2) Step 0a step 1 — argument shape rewritten to canonical `<finding-stem>::<target-form>::<headline-slug>`; cross-references to IB-163's writer-side spec made explicit. (3) Step 0a step 2 — queue-file resolution rewritten as scan-for-literal-heading-match across all `*.harvest-queue.md` files, with `--guide` restriction option, zero-match abort, multi-match abort, and queue file path caching for downstream Step 4.8 atomic-write. (4) Step 0a step 3 — literal heading match retained but updated to use the canonical heading shape and reference Step 2's already-resolved row identity; defensive recheck described. (5) Step 0a step 6 — `harvest_row_id` cached value updated to canonical shape; resolved queue file path also cached for Step 4.8. (6) Step 4.8 Branch A step 1 — queue-file path source updated from formula-based (`extracts/guides/<guide-stem>.harvest-queue.md`) to cached (per Step 0a step 2 auto-locate). (7) Step 4.8 Branch A step 7 — report-string `<guide-stem>.harvest-queue.md` substituted with `<resolved-queue-filename>`. (8) Failure-modes table — pre-existing 'reference resolves to non-existent row' row updated to cite Step 0a step 2 zero-match scan (not step 3 row-locate); new 'multi-match' row added citing Step 0a step 2 multi-match scan + `--guide` recovery. (9) Design Decisions table — DD-101 row appended with 'IB-166 amendment (session 82)' clause documenting the argument-shape canonicalization + auto-locate resolution + `--guide` disambiguation flag, and explicitly noting that this replaces IB-164's `<guide-stem>::...` argument shape. **Acceptance addressed.** (i) Real `--harvest-row dark-code-organizational-capability-problem::rule::ship-only-what-at-least-one-human-comprehended` invocation against G9 queue: argument parses, Step 0a step 2 scans queue files, finds exactly one match in `agent-governance-and-trust.harvest-queue.md`, caches the path, Step 0a step 3 confirms heading-match, Step 0a step 4 reads row fields, Step 0a step 5 confirms `nick-approved` Status, Step 0a step 6 builds synthetic finding entry with cached queue file path → downstream pipeline runs. (ii) Hypothetical multi-match (same `(finding-stem, target-form, headline-slug)` tuple in two queue files): Step 0a step 2 multi-match abort fires; user re-invokes with `--guide <guide-stem>` to disambiguate; resolution succeeds. (iii) Hypothetical zero-match (typo'd headline-slug): Step 0a step 2 zero-match abort fires; user corrects argument and re-invokes. **Executor's-choice resolutions.** (1) Argument prefix: `<finding-stem>` (matches IB-163 canonical shape). Considered alternative: amend writer-side to use `<guide-stem>` heading prefix and re-write all 38 row headings across 3 queue files; rejected (larger blast radius; mutates audit-trail rows; requires writer + consumer changes; IB-163 closure already established `<finding-stem>` as canonical and this amendment respects that source-of-truth). (2) Queue-file resolution: auto-locate by scan with `--guide` disambiguation flag. Considered alternative: require `--guide` flag on every harvest-row invocation (mandatory two-flag pattern); rejected (auto-locate is the minimum-viable mechanism and matches Occam's-razor preference; `--guide` is opt-in for the rare multi-match edge case; cross-guide collision is OOS per IB-163 closure note (c) so multi-match is hypothetical for now). (3) Failure-modes table — added new multi-match row rather than amending existing zero-match row, since the two abort conditions are distinct and have different recoveries. (4) DD-101 row in DD table — appended an 'IB-166 amendment (session 82)' clause rather than overwriting the IB-164 description, preserving the audit trail of how the contract evolved. **Bugs surfaced.** None beyond the documented defect. **Contract amendments proposed/applied.** Inline same-session amendment per (1)–(9) above. **Logged for future.** (a) Symmetry check on `/extract-artifacts` other harvest-mode references in failure-modes + Design Decisions table — verified all paths now consistently reference `<finding-stem>` shape and cached queue path; no remaining `<guide-stem>` references except the IB-166-amendment clause itself (which correctly describes the historical broken shape for audit purposes). (b) Verification of next harvest-row invocation under the patched SKILL.md — session 82's 24-row batch run is the live verification; if any invocation aborts at Step 0a step 2/3 unexpectedly, this IB reopens. (c) IB-164 closure status — IB-164 closed pre-execution; IB-166 amends its consumer-side contract. IB-164 status not changed; IB-166 documents the amendment as a separate audit-trail entry. Future readers of IB-164 should note the cross-reference: 'see IB-166 for argument-shape canonicalization'. (d) Cross-guide collision case is OOS per IB-163 closure note (c) but the auto-locate scan now defensively detects it; if it ever fires in practice, file follow-up IB to formalize cross-guide deduplication semantics."
milestone: null
---

# IB-166: /extract-artifacts argument shape mismatch with IB-163 row-heading shape

Discovered + closed in session 82 during IB-164's first-execution session-start structural verification (the verification specifically flagged in IB-163 closure note (a) as load-bearing at IB-164 execution time). `/extract-artifacts` SKILL.md Step 0a's `--harvest-row` / `--harvest-dismiss` argument shape declared `<guide-stem>::<target-form>::<headline-slug>` and demanded a literal heading match against the same shape; but IB-163 + `/synthesize-guide` Step 4.7 writes the canonical row heading as `### <finding-stem>::<target-form>::<headline-slug>`. Under the IB-164-as-shipped contract, no real `--harvest-row` argument could resolve to a real row heading. Closed by patching `/extract-artifacts/SKILL.md` inline: argument shape canonicalized to `<finding-stem>::<target-form>::<headline-slug>` (matches IB-163 writer-side); Step 0a step 2 queue-file resolution auto-locates by literal heading match across `extracts/guides/*.harvest-queue.md`; new optional `--guide <guide-stem>` flag for the multi-match disambiguation case; failure-modes table + Design Decisions DD-101 row updated; queue-file path now cached at Step 0a step 2 for downstream Step 4.8 atomic-write. Single same-session amendment, Nick-gated. Acceptance verified by session 82's harvest-row invocations resolving cleanly under the patched contract.
