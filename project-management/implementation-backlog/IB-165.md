---
name: "/synthesize-guide Step 4.7 queue-write template regression — slim 3-field per-row blocks emitted in sessions 78 + 79"
id: "IB-165"
source_dd: "DD-101"
status: "Done"
target_system: "improvement-loop"
priority: "P3"
type: "Fix"
notes: "Discovered + closed session 81 during the harvest-queue-rulings session-start structural verification pass. **Defect.** Sessions 78 (G2 queue, 16 rows) and 79 (G9 queue, 14 rows) wrote per-row details blocks carrying only 3 of the 9 DD-101-required fields (Source excerpt, Codifier's reading, Resolution); missing fields were Date queued, Status, Target form, Source finding, Suggested headline, Recommendation. Session 77's G7 queue (8 rows) was conformant. Data was intact in the summary-table surface; the defect was confined to the per-row-block surface. **Root cause.** SKILL.md Step 4.7 (Item 2.a) enumerated the 9 required fields textually but provided no rendered exemplar of the per-row markdown shape. The summary-table header was exemplified, but the per-row block was not. Sessions 78/79's LLM extrapolated from the field list with maximum compression; session 77 happened to render the full field list in canonical bulleted form. **Closure mechanism (session 81).** (1) SKILL.md Step 4.7 patched: added a 9-field per-row block exemplar (immediately after Item 2.a's heading-shape line) labeled 'renders ALL 9 required fields — copy this shape; do not abridge', plus a 6-column summary-table row exemplar, plus a 'both surfaces required' invariant statement clarifying that summary-table and per-row block must agree on the (source_finding, target_form, headline-slug, status, recommendation) tuple at all times. Future regens have an unambiguous template to anchor on. (2) G2 queue file backfilled inline to G7's full 9-field shape (16 rows × 6 missing fields backfilled from summary-table + heading data). (3) G9 queue file backfilled identically (14 rows). (4) Backfill verified via grep field-completeness check: all 38 rows now carry 9 of 9 required fields per the DD-101 §Queue file shape contract. **Override of standing rule.** Inline-fix of G2/G9 deviated from the standing 'do not inline-fix; file follow-up IB' rule per Nick's explicit session-81 authorization (rationale: rulings session would have been blocked otherwise; defect was structurally confined and source data was unambiguous from the summary tables). The SKILL.md patch addresses the source-level cause; even after future regens, the slim-block emission cannot recur. **DD-101 exemplar inconsistency — deferred.** During the SKILL.md patch, surfaced that DD-101.md's own per-row block exemplar (lines 60-73) renders 8 fields (omits Target form as a body field; Target form is encoded only in the heading suffix), contradicting DD-101.md:81's authoritative 9-field requirement. Three options weighed: (1) DD-44 supersession of DD-101; (2) file IB to track for batched supersession; (3) accept as-is. Chose (3) per Nick session-81 ruling — operative defect is closed at the writer (SKILL.md), DD-101.md:81 field-list text is authoritative over its illustrative exemplar, DD-44 supersession ceremony is disproportionate for a 2-day-old DD's pedagogical drift, and standing tolerate-one-off discipline applies (first DD-internal exemplar inconsistency surfaced). One-line audit-trail note added to session-81 SL: 'discovered, deferred under tolerate-one-off; revisit if recurs'. If a second DD-vs-spec-text inconsistency surfaces later, escalates to IB-track for batched supersession. **Self-check declined.** Considered adding a Step 4.7 atomic-write self-check that verifies all 9 per-row fields are present before commit. Declined per tolerate-one-off discipline; SKILL.md exemplar should anchor future regens cleanly. Revisit if recurrence on next regen-active session. **Bugs surfaced.** None beyond the documented defect. **Contract amendments.** None. **Logged for future.** (a) DD-101 exemplar inconsistency — see above; deferred; revisit-if-recurs. (b) Step 4.7 atomic-write self-check — see above; declined this session; revisit on recurrence. (c) Verification of next regen-active session's queue-write output — confirms the SKILL.md patch holds under live invocation. Trigger: next `/synthesize-guide` run that generates harvest-queue rows. (d) Session-81 calibration histogram landed 100% Codifier-recommendation accuracy across 38 rulings (24 extract-rec + 14 dismiss-rec all accepted verbatim by Nick). Sample size still small (one session); not yet a calibration baseline."
milestone: null
---

# IB-165: /synthesize-guide Step 4.7 queue-write template regression

Discovered + closed in session 81 during the harvest-queue-rulings session-start structural verification. Sessions 78 (G2) and 79 (G9) emitted slim 3-field per-row details blocks instead of the DD-101-required 9-field shape; session 77 (G7) was conformant. Root cause: SKILL.md Step 4.7 enumerated the field list but provided no rendered exemplar of the per-row markdown shape, allowing LLM extrapolation drift. Closed by patching Step 4.7 with an explicit 9-field exemplar + summary-table row exemplar + both-surfaces-required invariant, plus inline backfill of G2/G9 queue files (Nick override of standard "do not inline-fix" rule). DD-101 exemplar inconsistency surfaced as a side-effect; deferred per tolerate-one-off.
