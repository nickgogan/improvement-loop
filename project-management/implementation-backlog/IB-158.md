---
name: "Add corpus-scan + extension-proposal step to /extract-artifacts (DD-97)"
id: "IB-158"
source_dd: "DD-97"
status: "Open"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Codifier scope. Update `.claude/skills/extract-artifacts/SKILL.md` to add a corpus-scan step BEFORE drafting any new rule or skill artifact. Procedure: when the assigned form is `rule`, scan `extracts/rules/*.md`; when `skill`, scan `extracts/skills/*.md`; templates and agents skip this step (out of scope per DD-97 §Scope). Calibration: LLM judgment, loose — no structured threshold. Skill reads candidate finding + corpus, judges semantic similarity over titles, summaries, ContractSpec content, body language. On match, emit structured extension proposal (do NOT auto-merge, do NOT create new): (a) the candidate finding's relevant content, (b) the existing artifact named, (c) a diff sketch — for rules: appended Evidence row; for skills: added mode flag with new variant's behavior, (d) Codifier recommendation from closed enum (`extend existing` | `create new (false positive)` | `parameterize as mode variant`). On no-match, proceed to create new artifact per pre-DD-97 behavior (no behavioral change). Multiple plausible matches: surface strongest match + flag secondaries; Nick rules merge target. Acceptance: 6 cases from DD-97 §Acceptance Criteria — overlap rule case proposes extension; mode-variant skill case proposes parameterization; no-match case creates new (regression check); multiple-match case flags secondaries; auto-merge attempt is procedural violation; template/agent classification skips the rubric. Phase-2 lifecycle bundle (alongside IB-157). Calibration is v1 (loose); promotion to (ii) ContractSpec-overlap-structured or (iii) hybrid requires a follow-up DD if false-positive volume justifies it."
milestone: null
---
