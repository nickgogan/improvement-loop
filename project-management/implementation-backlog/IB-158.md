---
name: "Add corpus-scan + extension-proposal step to /extract-artifacts (DD-97)"
id: "IB-158"
source_dd:
  - "DD-97"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 71 (Phase B scope expansion). Applied to `.claude/skills/extract-artifacts/SKILL.md`: added new Step 1.7 `Corpus Scan + Extension Proposal (DD-97) — rule and skill forms only` between Step 1 (Filter to Approved) and Step 2 (Draft Artifacts). Calibration (i) — LLM judgment, loose; no structured threshold. Procedure: partitions findings into `rule_findings` and `skill_findings` (templates and agents skip Step 1.7 entirely per DD-97 §Scope); globs the matching corpus (`extracts/rules/*.md` or `extracts/skills/*.md`, skip `_index.md`); per-finding LLM similarity check over title + summary + body + ContractSpec content; categorizes each as `extension_status: \"no_match\"` (proceeds to Step 2 drafting per current behavior — no behavioral change), `\"proposed\"` (single match — emits one extension proposal and skips drafting), or `\"proposed\"` with secondaries (multi-match — surfaces strongest match as primary + flags other candidates as secondaries). Per-proposal block carries: candidate finding stem, primary + secondary existing artifacts, Codifier recommendation from closed enum (`extend existing` | `create new (false positive)` | `parameterize as mode variant`), why-this-match (1–2 lines explaining the LLM-observed semantic overlap), diff sketch (rule: appended Evidence row with new source finding cited alongside original; skill: added mode flag with new variant's behavior + ContractSpec invariant additions covering the new mode), optional notes line. Aggregated proposals written to per-run report at `operations/extension-proposals/<YYYY-MM-DD>-extension-proposals.md` with frontmatter (counters, source identification report, forms_scanned). If zero proposals emitted, report file NOT written and skill reports passthrough. Auto-merge prohibition codified — Step 1.7 NEVER modifies any existing artifact; only file written is the proposals report. Step 2 amended to filter out `extension_status: \"proposed\"` findings before drafting. Paths table gains `operations/extension-proposals/` (created on first run). Failure-modes table gains three rows (auto-merge attempt, scope violation on template/agent, multi-match silent-pick). Design Decisions table gains DD-97. All 6 acceptance cases from DD-97 §Acceptance Criteria addressed by procedure design — overlap rule case proposes extension; mode-variant skill case proposes parameterization; no-match case proceeds to drafting unchanged (regression check); multi-match case surfaces primary + secondaries; auto-merge attempt is procedural violation; template/agent classification skips the rubric. Resolved ambiguity (documented in session-71 SL Phase B): DD-97 §Rules #4 references re-running `/extract-artifacts` to apply an extension but doesn't specify the apply path — Resolution for v1: Step 1.7 only emits proposals; applying an extension is a manual edit guided by the proposal's diff sketch. Promoting the apply path to skill-native behavior is a future IB. Bugs surfaced: none. Amendments proposed: none. Logged for future: calibration tightening from (i) LLM-loose to (ii) ContractSpec-overlap-structured or (iii) hybrid if false-positive volume becomes burdensome — Nick-observed trigger; not pre-emptive. Extension-application skill behavior promotion if proposal volume becomes high (>5/run). Live validation deferred to first /extract-artifacts run against an identification report containing rule/skill candidates. Phase-2 lifecycle bundle (alongside IB-157)."
milestone: null
---
