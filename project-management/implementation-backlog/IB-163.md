---
name: "Implement /synthesize-guide co-occurrence harvest-queue scan + per-guide queue file (DD-101)"
id: "IB-163"
source_dd: "DD-101"
status: "Queued"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Phase-3 lifecycle bundle (alongside IB-159, IB-160, IB-161, IB-162, IB-164). Implements DD-101's writer side — `/synthesize-guide` scans pattern findings during absorption for embedded artifact-shaped content and appends candidates to a per-guide harvest queue. Read-only by contract: never auto-extracts (IB-164 is the downstream Nick-gated promotion path). **Scope.** Two work items, both required for DD-101's queue mechanism to be operative on the next guide regen. **Item 1 — Absorption-phase co-occurrence scan.** Add scan logic to `/synthesize-guide`'s absorption phase (the section of the skill that reads pattern findings to build the guide body — verify exact step number against the current SKILL.md; likely Step 2 or Step 3 per the guide-synthesis pipeline). For each pattern finding being absorbed, examine the finding's body for prose that reads as artifact-shaped content of a non-pattern form per DD-101 §The Constraint: **rule shape** (imperative directive — 'never X', 'always Y'; machine-enforceable; fits rule artifact form per the form-classification rubric); **skill shape** (a procedure with input/output, invocation contract, step-by-step structure); **template shape** (a structural scaffold meant for rendering — frontmatter blocks, placeholder fields, structural form). Detection is **LLM-loose**, calibrated to match DD-97's extension-rubric philosophy — false-positives tolerable (cost = one Nick gate per row); false-negatives tolerable (DD-77 explicitly accepts read-time consumer-driven resolution; other consumers may surface what `/synthesize-guide` misses). **Agent suppression invariant.** If detection would surface agent-shaped content, the candidate is logged inline in the synthesis report (NOT the queue) and DD-82 governs separately — the queue NEVER carries agent-form rows. This is structural, not advisory: enforce in the row-write path with a closed-enum target-form check (`rule` | `skill` | `template`; agent rejected). **Item 2 — Per-guide queue file write + duplicate-suppression + supersession.** For a guide at `extracts/guides/<guide-stem>.md`, the harvest queue file lives at `extracts/guides/<guide-stem>.harvest-queue.md` (mirrors DD-94's companion changelog convention `<guide-stem>.changelog.md` — same per-guide locality argument; same `<guide-stem>.<purpose>.md` pattern). Create the file if absent on first detection; the file's header per DD-101 §Queue file shape: title heading `# Co-occurrence Harvest Queue — <Guide Title>`; explanatory note ('Embedded artifact candidates surfaced during `/synthesize-guide` runs. Per DD-101. Nothing here is auto-extracted; rows feed `/extract-artifacts` only after Nick rules.'); summary table with columns (Date queued | Status | Target form | Source finding | Suggested headline | Recommendation); per-row details section below the table. **Per-row content fields (required, per DD-101 §The Constraint).** Date queued (ISO date); Status (closed enum: `queued` | `nick-approved` | `nick-dismissed` | `extracted` | `superseded`); Target form (closed enum: `rule` | `skill` | `template`; agent excluded); Source finding (the pattern finding whose body contained the embedded prose); Source excerpt (verbatim quote ≤8 lines, ellipsis with structural anchor preserved for longer); Codifier's reading (1–3 lines explaining why this prose reads as the target form, citing form-rubric criteria when relevant); Suggested headline (Codifier's proposed artifact title); Recommendation (closed enum: `extract via /extract-artifacts` | `dismiss as inline` | `merge into existing [[<artifact-stem>]]`); Resolution (filled by IB-164 / Nick rulings; closed enum: `extracted to [[<artifact-stem>]]` | `dismissed` | `merged into [[<artifact-stem>]]` | `superseded`). **Duplicate suppression at write.** Before appending a new row, check for an existing row with the same (source_finding, target_form) tuple. If a row exists with status `queued`, `nick-dismissed`, `extracted`, or `superseded`, NO new row is appended — the prior row stands. This prevents queue-bloat from regen cycles re-detecting the same embedded prose per DD-101 §The Constraint. **Append-only across regen cycles.** Each `/synthesize-guide` regen scans pattern findings for the (now-current) cluster. New rows append (most-recent-last in the table; per-row details append in document order). Existing rows persist with their current status — status transitions are by Nick's ruling and IB-164's downstream `/extract-artifacts` writes, NOT by `/synthesize-guide` activity. Rows are NEVER deleted. **Supersession on cluster departure.** If a regen of the guide finds that a previously-absorbed pattern finding is no longer in the cluster (e.g., dimension-rebalance moved it elsewhere — see IB-153), the skill marks every `queued` or `nick-approved` row sourced from that finding as `superseded` (NOT `nick-dismissed` — the supersession reason is structural, not Nick-judgmental). `extracted` rows remain `extracted` (the artifact exists; the source's later movement does not invalidate it). `nick-dismissed` rows remain `nick-dismissed` (Nick's ruling stands). The supersession annotation cites the regen's session number and SL stem; the row is not deleted. **Item 3 — Procedure-table + DD-table updates.** Failure-modes table gains rows for: (i) queue-write failure mid-regen; (ii) agent-shaped content detection (must suppress queue emission and log inline; verify the suppression path); (iii) duplicate-suppression collision (existing row with same tuple — verify suppression activates); (iv) supersession-on-departure path (verify status update for `queued`/`nick-approved` rows; verify non-update for `extracted`/`nick-dismissed` rows). Design Decisions table gains DD-101. **Rules (subset of DD-101 §Rules for `/synthesize-guide`).** (1) Scan during absorption phase, per pattern finding. (2) LLM-loose calibration; no structured threshold. (3) Suppress agent candidates from queue; log inline in synthesis report. (4) Append to per-guide queue file; create file if absent with the standard header. (5) Duplicate suppression on (source_finding, target_form) tuple. (6) Append-only across regen; status transitions are Nick-rulings and IB-164 writes, NOT skill activity. (7) Never auto-extract — extraction requires Nick's ruling + separate `/extract-artifacts` invocation per IB-164. (8) Mark superseded on cluster departure; cite regen session + SL stem; never delete the row. **Acceptance Criteria (subset of DD-101 §Acceptance Criteria).** (i) `/synthesize-guide` regen on a pattern finding whose body contains a clear rule sentence — harvest queue file created (or appended to); row status `queued`, target form `rule`, source finding pointer set, source excerpt captured; (ii) Same regen, multiple findings each carrying embedded rules — multiple rows appended; one per (source_finding, target_form) tuple; (iii) Two consecutive regens of the same guide with no source-finding changes — second regen detects the same embedded rules; duplicate suppression prevents row re-emission; queue size unchanged; (iv) Pattern finding contains agent-shaped content — NO row appended for `agent` form; inline narrative in synthesis report only; (v) Source pattern finding removed from cluster on next regen; row was `queued` — row status updated to `superseded`; row retained; no extraction performed; (vi) Codifier auto-extracts from a queue row with status `queued` (no Nick ruling) — procedural violation, flagged in next governance audit (and IB-164 enforces this from the `/extract-artifacts` side). **Dependencies.** IB-154 (DD-93 preserved-section enforcement on `/synthesize-guide` — IB-163's queue-write occurs alongside the same-skill regen path). IB-155 (DD-94 changelog appender — IB-163's queue file is a structural sibling using the same per-guide companion-file pattern). DD-77 (single-form classification — IB-163's queue is the operationalization for one specific consumer). DD-82 (agent never-auto-create invariant — IB-163's agent-suppression path enforces it). Blocks IB-164: queue file with `nick-approved` rows is the input shape IB-164's `/extract-artifacts` reader path consumes. **Touch Points.** `.claude/skills/synthesize-guide/SKILL.md` (new absorption-phase scan step; queue-write logic; duplicate-suppression check; supersession-on-departure check; failure-modes additions; DD table addition); `extracts/guides/` directory (one new `<guide-stem>.harvest-queue.md` file per guide that has co-occurrence detections — created on first detection per guide, not pre-created; not all guides will accumulate queue files). NO touches to existing guide bodies; queue file is a separate companion artifact. **Form precedent.** IB-155 Item 1 (Step 4.5 appender to `/synthesize-guide` writing a per-guide companion file with closed-enum trigger tags + duplicate-handling semantics — IB-163's structural twin); IB-154 (procedure-step inserts to `/synthesize-guide` with capture/restore/regression semantics — IB-163's queue-write is a similar in-skill artifact emission)."
milestone: null
---
