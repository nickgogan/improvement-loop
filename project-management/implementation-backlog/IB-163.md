---
name: "Implement /synthesize-guide co-occurrence harvest-queue scan + per-guide queue file (DD-101)"
id: "IB-163"
source_dd: "DD-101"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 76. **Item 1 — Absorption-phase co-occurrence scan.** Inserted new Step 4.7 in `.claude/skills/synthesize-guide/SKILL.md` between Step 4.5 (DD-94 changelog) and Step 5 (synthesis status / cross-references). Step 4.7 owns: per-finding LLM-loose scan over the absorbed pattern findings for embedded artifact-shaped content of three target forms (`rule` | `skill` | `template`); agent-shape suppression (logged inline in run report; queue NEVER carries agent rows); per-guide queue file write + duplicate suppression + supersession-on-departure. Calibration parity with DD-97 and Step 1.8 of `/extract-artifacts`. Executor's choice on step number resolved: Step 4.7 (after Step 4 guide-write so the kebab-case guide-stem is finalized; before Step 5 cross-references; after Step 4.5 changelog so the regen's primary reporting surfaces are in place; queue-write is a parallel companion-file emission that mirrors Step 4.5's per-guide-companion structural pattern). **Item 2 — Per-guide queue file write + duplicate suppression + supersession.** Queue file location `extracts/guides/<guide-stem>.harvest-queue.md` (mirrors DD-94's `<guide-stem>.changelog.md` per-guide-locality convention). Header documented per DD-101 §Queue file shape (title heading + explanatory note + summary table + `## Per-row details` section). Per-row required fields documented per DD-101 §The Constraint: Date queued, Status (closed enum: `queued` | `nick-approved` | `nick-dismissed` | `extracted` | `superseded`), Target form (closed enum: `rule` | `skill` | `template`), Source finding (wikilink), Source excerpt (verbatim ≤8 lines), Codifier's reading (1–3 lines), Suggested headline, Recommendation (closed enum: `extract via /extract-artifacts` | `dismiss as inline` | `merge into existing [[<artifact-stem>]]`), Resolution (closed enum: `extracted to [[<artifact-stem>]]` | `dismissed` | `merged into [[<artifact-stem>]]` | `superseded`; left blank on new rows). Per-row details block heading uses compound structural ID `### <finding-stem>::<target-form>::<headline-slug>` — this is IB-164's queue-row reference shape AND the duplicate-suppression-display ID; the duplicate-suppression KEY itself is `(source_finding, target_form)` (headline slug NOT part of the key). Duplicate suppression at write: any existing row with matching tuple AND status in `{queued, nick-dismissed, extracted, superseded}` suppresses the new row write — terminal-status rows DO suppress re-emission per DD-101 §The Constraint (prevents queue bloat). Supersession-on-departure: comparing prior on-disk `source_findings[]` (cached during Step 0.5 alongside DD-93 preservation capture) against current absorbed set; for departed findings, all `queued` and `nick-approved` rows whose source matches are flipped to `superseded` with regen-session + SL-stem footer annotation; `extracted` and `nick-dismissed` rows are NOT touched (audit-trail invariant: extracted artifacts persist; Nick rulings stand). Append-only invariant: rows never deleted; `/synthesize-guide` only writes new rows or supersession-marks departed-source rows; status transitions for Nick rulings + IB-164 extractions are downstream. Initial-synthesis behavior: queue file created lazily on first qualifying detection (mirrors DD-94 changelog initial-synthesis lazy-create behavior). Atomic-write: read-existing → construct-updated → write. **Item 3 — Procedure-table + DD-table updates.** Failure-modes table gains 4 new rows: (i) queue-write failure mid-regen (procedural failure; recovery via manual re-run or next-regen re-detect; flag as governance audit item if recurring); (ii) agent-shape content detected (suppress per Item 1; log inline only; abort row write with structured error citing DD-82 + DD-101 §Rules for `/synthesize-guide` item 3 if a queue write is attempted with `target form: agent`); (iii) duplicate-suppression collision (verify the suppression activates; surface in run report's D count); (iv) source-finding cluster departure path (verify supersession marks `queued`/`nick-approved` only; verifies non-mutation of `extracted` and `nick-dismissed`). Design Decisions table gains DD-101 (full row citing Step 4.7 + closed enums + dup-suppression + append-only + supersession + read-only-by-contract) AND DD-82 (three-layer harvest-layer enforcement: enum-target-rejection + inline-narrative-only + downstream-defensive-abort). **Acceptance addressed.** (i) Pattern finding with clear rule sentence → harvest queue file created/appended; row status `queued`, target form `rule`, source finding pointer set, source excerpt captured — Item 1 + Item 2 + Item 2.a. (ii) Multiple findings with embedded rules → multiple rows appended; one per `(source_finding, target_form)` tuple — Item 2.a + Item 2.b. (iii) Two consecutive regens with no source-finding changes → second regen detects same embedded rules; duplicate suppression prevents row re-emission; queue size unchanged — Item 2.b. (iv) Pattern finding with agent-shape content → NO row appended for `agent` form; inline narrative in run report only — Item 1 agent-suppression invariant. (v) Source pattern finding removed from cluster on next regen; row was `queued` → row status updated to `superseded`; row retained; no extraction — Item 2.c. (vi) Auto-extract from `queued` row procedural violation → flagged in next governance audit (and IB-164 enforces the same on `/extract-artifacts` side) — covered in failure-modes table + DD-101 row in DD table (read-only by contract). **Executor's-choice resolutions.** (1) Step number: Step 4.7 (after Step 4 guide-write, before Step 5 cross-references, sibling to Step 4.5 changelog). Reasoning: needs finalized guide-stem (post-Step-4); needs prior-source_findings cache for supersession (cached at Step 0.5 alongside DD-93 captures); structural twin to Step 4.5 per-guide-companion-file emission. (2) Per-row details heading shape: `### <finding-stem>::<target-form>::<headline-slug>` — chosen for both human-readable navigation AND IB-164 queue-row reference simplicity; duplicate-suppression key kept narrower at `(source_finding, target_form)` so headline drift between regens does NOT trigger re-emission. (3) Supersession annotation form: append-footer-line on per-row details block (preserves audit history without rewriting the row body). **Bugs surfaced.** None. **Contract amendments proposed/applied.** None. **Logged for future.** (a) IB-164's `/extract-artifacts` queue-row consumer mode reads queue rows by the compound heading ID — verify the heading shape matches IB-164's argument naming convention at IB-164 execution; this IB establishes `<finding-stem>::<target-form>::<headline-slug>` as the canonical row ID. (b) The Step 0.5 prior-`source_findings[]` cache (added in IB-154 for DD-93 preservation) is reused at Step 4.7 for supersession-on-departure; this is an IB-154 ↔ IB-163 read dependency that becomes load-bearing at Step 4.7. If Step 0.5's cache shape changes in a future IB, Step 4.7 must adapt. (c) Cross-guide deduplication (same embedded rule detected in two different guides' queue files) is explicitly OUT of scope per DD-101 §Scope and Non-Goals — IB-164's `/extract-artifacts` DD-97 path catches cross-artifact overlap at extract time. The queue is per-guide; cross-guide consolidation is downstream. (d) Run-report wording for the SU (superseded) count is implementation-suggestion; future regen output may benefit from listing the superseded row IDs explicitly if the count rises above ~5 per regen — defer until observed. (e) The compound row-heading ID format may benefit from formal codification in DD-101 amendment if other consumers (beyond IB-164) need stable references — defer until a second consumer surfaces."
milestone: null
---
