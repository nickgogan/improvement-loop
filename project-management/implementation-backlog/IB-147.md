---
name: "Build /extract-artifacts skill — harvest derivative forms from codified patterns"
id: "IB-147"
source_dd: "DD-45"
status: "Cancelled"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "New skill that periodically reviews codified patterns in meta-system/knowledge/patterns/ and proposes derivative artifacts that could be extracted: skills derivable from a pattern, templates derivable from several related patterns, prompting insights, or guides synthesizable toward a specific end. Decouples 'codify the pattern' (main pipeline) from 'harvest derivatives' (this skill), so the Form Router doesn't have to decide ambiguous secondary form cases at codification time. Motivated by the artifact-design session on 2026-04-10, specifically the Finding 3 (MCP as Code API) paper exercise where a pattern had a legitimate secondary template candidate that we chose to defer rather than force. Pairs with /synthesize-guide (IB-146) — both run as periodic post-codification harvesters. Acceptance: (1) skill file at .claude/skills/extract-artifacts/SKILL.md; (2) takes a pattern ID or a set of pattern IDs as input; (3) produces a proposal document listing candidate derivative artifacts (form + rationale + estimated utility) for human review; (4) does NOT autonomously write derivative artifacts — only proposes. Context: systems/improvement-loop/operations/loop-reports/2026-04-10-architect-handoff-deep-research-prompt.md and the aiAgentsFocus results file in the same folder."
milestone: null
---

# IB-147: Build /extract-artifacts skill

## Closure — session 123 (2026-06-19)

**Cancelled — superseded by DD-80.** The original "harvest derivative forms / propose-don't-write" purpose was redesigned away. DD-80's `/extract-artifacts` (drafts and *writes* staged artifacts from an approved `/identify-artifacts` report) reuses the name for a structurally different skill. DD-80 frontmatter already lists IB-147 in `ib_items`. Verified the live skill against the original acceptance criteria — they do not match; this is a name reuse, not a fulfilment.
