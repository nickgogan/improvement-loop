---
name: "Implement /extract-artifacts queue-row promotion path + status updates (DD-101)"
id: "IB-164"
source_dd:
  - "DD-101"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 76. **Item 1 — Queue-row input mode.** Two new arguments added to `/extract-artifacts`: `--harvest-row <guide-stem>::<target-form>::<headline-slug>` (promotion mode) and `--harvest-dismiss <guide-stem>::<target-form>::<headline-slug>` (queue-only dismissal mode). Argument shape follows IB-163's compound row-heading ID (`<finding-stem>::<target-form>::<headline-slug>` per row's `### ` heading; here `<guide-stem>` prefix is the guide's stem since rows live in per-guide queue files). Step 0 amended to dispatch on three mutually-exclusive invocation modes: (a) identification-report (default; `<report-filename>` or `--latest`); (b) harvest-queue promotion (`--harvest-row`); (c) harvest-queue dismissal (`--harvest-dismiss`). Multi-mode invocations abort. New Step 0a (Resolve Harvest Queue Row) fires only in harvest mode: parse compound ID, resolve queue file path, locate per-row details block by exact heading match, validate target form is in closed enum {rule, skill, template} (agent → defensive abort citing DD-82 + IB-163 invariants), apply mode-specific Status check (promotion: `nick-approved` only; dismissal: `queued` or `nick-approved`; reject `extracted`/`superseded`/`nick-dismissed` with status-specific informational notes), build synthetic single-entry finding for promotion mode (id = source_finding stem; assigned_form = target form; Status = APPROVED; harvest_row_id cached for Step 4.8 write-back; harvest_source_excerpt + Codifier's reading + suggested headline cached as drafting input). Single-entry contract: harvest mode processes EXACTLY one row per invocation (multi-row batching erodes per-row gate; sequential invocations for multi-row work). The skill NEVER auto-polls harvest queues — explicit flag is the only entry path. **Item 2 — Downstream DD chaining.** Promotion-mode pipeline: Step 0 → Step 0a → Step 1 (filter passes the synthetic entry through; pattern filter inapplicable since target form is rule/skill/template) → Step 1.7 fires for rule/skill (DD-97 corpus scan; may emit extension proposal) → Step 1.8 Branch A fires for template (DD-100 corpus scan; may emit version-bump proposal) → Step 2 (drafts if no proposal emitted) → Step 2.5 (validate) → Step 2.7 (DD-95 lifecycle pointer; required even in harvest mode for consistent SL chain) → Step 3 (write artifact, possibly with versioned-write branch for DD-100 version-bump-applied path) → Step 4 (back-annotate source finding) → Step 4.8 (queue write-back) → Step 5 (back-annotate finding pipeline_status; reuses existing 'synthesized → keep, append to consumed_by[]' logic — original pattern finding remains pattern-classified per DD-101 §Promotion; its `consumed_by[]` gains the new harvested artifact's pointer alongside the original guide's). Defensive: agent target form aborted at Step 0a; never reaches Step 1+. **Item 3 — Queue row status update on extraction / dismissal / merge.** New Step 4.8 (Harvest-Queue Row Write-back) inserted between Step 4 and Step 5. Four operative branches plus a defensive E branch. Branch A (--harvest-dismiss): direct from Step 0a (skip Steps 1-4 entirely); update Status → `nick-dismissed`, Resolution → `dismissed`; append dismissal footer with regen session + SL stem; row retained for audit. Branch B (--harvest-row + new artifact written): update Status → `extracted`, Resolution → `extracted to [[<artifact-stem>]]`; append extraction footer. Branch C (--harvest-row + DD-97 extension proposal emitted, no artifact this invocation): Status REMAINS `nick-approved`; Resolution remains blank; append pending-merge annotation citing the extension-proposals filename + primary-match artifact stem; manual apply per DD-97 v1 (Step 1.7 auto-merge prohibition); future skill-mode that closes the merge loop will flip Status. Branch D (--harvest-row + DD-100 version-bump proposal emitted, no artifact this invocation): Status REMAINS `nick-approved`; Resolution remains blank; append pending-version-bump annotation citing the version-bump-proposals filename + proposed `<existing-stem>-v<N+1>.md` filename; on Nick ruling, re-invocation routes to versioned-write path. Branch E (agent target): unreachable in normal flow (Step 0a aborts upstream); documented for completeness. Atomic-write invariant for all branches: read entire queue file, mutate only the named row's Status / Resolution / footer, write entire file back; all other rows preserved byte-equivalent; per-row details ordering preserved. **Item 4 — Procedure-table + DD-table updates.** Failure-modes table gains 11 new rows: (i) harvest-row + Status=`queued` (Nick gate not crossed); (ii) row reference resolves to non-existent row (typo / stale / wrong guide stem); (iii) target form `agent` (defensive abort + governance audit); (iv) harvest-row + Status `extracted`/`superseded` (already resolved — informational); (v) harvest-row + Status `nick-dismissed` (Nick ruled inline-only); (vi) harvest-dismiss on `extracted`/`superseded` (incoherent dismissal); (vii) harvest-dismiss on `nick-dismissed` (idempotent no-op); (viii) multi-mode invocations (mutually-exclusive flags); (ix) Branch B write-back failure mid-extraction (idempotent recovery: artifact exists, re-run skips drafting via Step 3 dedup and completes Step 4.8); (x) Branch A write-back failure mid-dismiss (idempotent recovery: re-run is no-op-equivalent); (xi) [implicit via Step 0 dispatch validation rows]. Design Decisions table gains DD-101 (full row citing input modes, Step 0 dispatch, Step 0a resolution, Step 4.8 four-branch write-back, atomic-write invariants, single-row contract, never-auto-poll, original-pattern-finding-preservation, DD-77 invariant honored at consumer-time). DD-82 row updated to multi-layer enforcement (added layer 4: Step 0a defensive abort on agent target). **Acceptance addressed.** (i) Nick rules `nick-approved` on a queued row; `/extract-artifacts --harvest-row <id>` runs → new rule artifact written (or DD-97 extension proposed); queue row updated per Branch B (or annotated per Branch C if extension) — full Step 0a → 1.7/1.8 → 3 → 4.8 path. (ii) Nick rules `dismiss as inline` → `--harvest-dismiss <id>` flips Status to `nick-dismissed`; row retained; no artifact created — Branch A. (iii) Auto-extract from `queued` row → procedural violation: Step 0a Status check aborts; flagged in failure-modes + DD-101 row. (iv) Rule target + similar existing rule → DD-97 extension proposal fires per Step 1.7 (existing IB-158 logic); row Resolution annotation captures proposed merge target — Branch C. (v) Template target + evolution match → DD-100 version-bump proposal fires per Step 1.8 Branch A; row annotation captures proposed v<N+1> filename — Branch D. (vi) Agent target → defensive abort at Step 0a citing DD-82 + DD-101 §Rules for `/synthesize-guide` item 3; never reaches drafting. **Executor's-choice resolutions.** (1) Argument naming: `--harvest-row` and `--harvest-dismiss` (matches IB suggestion verbatim; clear semantics; symmetric mode-flag pair). (2) Compound row-ID format: `<guide-stem>::<target-form>::<headline-slug>` (matches IB-163's row heading shape; uses `::` separator for visual distinction from path components). (3) Step numbering: Step 0a (resolution) + Step 4.8 (write-back) — Step 0a uses 'a' suffix (not 0.5) to avoid conflict with the existing decimal numbering convention which is reserved for in-line decimal procedure inserts (e.g., Step 1.7, 1.8, 2.5, 2.7); 0a is a mode-conditional pre-step. Step 4.8 sits between Step 4 (back-annotate source) and Step 5 (back-annotate finding pipeline_status); the queue is updated AFTER source back-annotation so the artifact's existence on disk is the precondition for the queue update. (4) Status check semantics: promotion-mode requires `nick-approved` (Nick's gate is the manual queue-file edit; skill enforces the gate post-edit); dismissal-mode accepts `queued` or `nick-approved` (Nick may dismiss before or after approving; both are coherent paths into dismissal). (5) Single-row contract: hard-coded; multi-row batching is achieved via sequential invocations to preserve per-row gate fidelity. **Bugs surfaced.** None. **Contract amendments proposed/applied.** None. **Logged for future.** (a) Branch C's manual-apply gap: DD-97 v1 leaves extension application as a manual edit (Step 1.7 auto-merge prohibition; documented in IB-158 closure). The queue row's Status flip from `nick-approved` to `extracted` post-merge is currently Nick-manual (or via Obsidian edit). A future IB may promote merge-application to skill-native behavior; that IB would also close the queue-row-flip loop atomically with the merge. Trigger: Nick observation of merge-volume (>5/run per IB-158 closure heuristic). (b) Branch D's version-bump-applied path: similarly, DD-100's version-bump-applied flow on harvest rows requires Nick to act on the version-bump proposal (rule version-bump, manually scope, or accept the proposal-as-instruction); the current skill design re-invokes through Step 1.8 Branch A's match-disambiguation path. A future IB may close this loop more tightly (e.g., a `--harvest-row + --version-bump-confirmed` flag combo), but not before the corpus exercises the path. (c) Step 6 Summary block does NOT currently surface harvest-mode counts (e.g., 'extracted from harvest queue: 1; dismissed from harvest queue: 0'). Polish-level addition; defer until first real harvest-mode invocation surfaces the gap. (d) Step 4 (Back-Annotate Source Findings) currently appends a fixed-format extraction note to the source finding's body. For harvest-row mode, the note's wording ('Extracted as **<form>**: [[<artifact>]] in `extracts/<form>s/`') is correct without modification — the source finding IS the original pattern finding, and the harvested artifact IS extracted as the row's target form. No amendment needed. Verified inline. (e) The cross-IB invariant (DD-77 'Router picks one form, full stop') is honored: harvest is consumer-side resolution at extract time; the original pattern finding's `assigned_form` (per Router) is unchanged; only `consumed_by[]` accumulates the harvested artifact's pointer alongside the original guide's. Verified at the Step 5 path level — existing 'pipeline_status: synthesized → keep' logic handles this correctly without skill amendment. **Session 85 amendment (2026-04-27) — subagent queue-mutation discipline architectural ruling.** Following session-83's procedural-deviation observation (multiple per-row `/extract-artifacts --harvest-row` subagents disregarded the orchestrator's 'DO NOT modify the queue file' instruction, with row 13 going the full SKILL.md Step 4.8 distance), Nick ruled **Option A — end-to-end subagent mode**, plus a partition-by-queue-file concurrency rule. Architectural decision: the skill's contract is that Step 4.8 runs in the same process as the artifact write (Step 3) and source back-annotation (Step 4); the harvest-mode pipeline is end-to-end per invocation. Orchestrators that spawn `/extract-artifacts --harvest-row` invocations as subagents in parallel MUST NOT themselves write the queue file post-batch (contradicts skill contract; splits atomicity across processes; creates contention). Concurrency rule: parallel batches MUST be partitioned by queue file — at most one active invocation per `extracts/guides/<stem>.harvest-queue.md` at any moment; within a queue file, rows are sequential; across queue files, parallelism is unbounded. Rationale: each Step 4.8 is per-process atomic read-modify-write of the entire queue file; two concurrent invocations against the same queue file race (both read pre-state, both write whole-file, last-writer-wins, loser's row update lost). Session-83 evidence: row 13's subagent demonstrated end-to-end execution works cleanly when honoring the skill contract. SKILL.md amendments applied this session: (1) Step 4.8 preamble extended with the 'End-to-end execution invariant' — Step 4.8 runs in the same process as the artifact write; atomicity from artifact write through queue flip preserved; orchestrators MUST NOT post-batch-write the queue file. (2) New subsection at end of Step 4.8 — 'Concurrency for parallel harvest-row batches' — partition-by-queue-file rule with rationale; orchestrator-contract enforcement (skill itself does not enforce). (3) Step 2 preamble extended with a two-subagent-pattern disambiguation table — drafting subagent (identification-report mode; this Step 2; no file writes; no queue touches) vs. whole-skill-invocation-as-subagent (harvest-mode parallel batches; full end-to-end pipeline including Step 4.8; spawned-skill-instance owns its own Step 4.8 per contract). The session-83 contention pattern is now contract-resolved: half-compliant subagents were the procedural violation; full-compliant subagents were honoring the skill contract. No new IB filed — the architectural decision is captured here as a session-85 amendment to IB-164 and in the session-85 SL."
milestone: null
---
