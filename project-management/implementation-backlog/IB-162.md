---
name: "Implement /extract-artifacts template version-bump path + agent flag-only path (DD-100)"
id: "IB-162"
source_dd: "DD-100"
status: "Queued"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Phase-3 lifecycle bundle (alongside IB-159, IB-160, IB-161, IB-163, IB-164). Implements DD-100's writer-side version-bump path on `/extract-artifacts` for templates, plus the asymmetric agent flag-only path per DD-82's never-auto-create invariant. **Scope.** Extends `.claude/skills/extract-artifacts/SKILL.md` with two new procedure paths, structurally analogous to IB-158's Step 1.7 corpus-scan + extension-proposal pattern (DD-97 for rules/skills) but targeting templates with version-bump semantics rather than extension semantics. **Item 1 — Template version-bump corpus scan.** Add a new procedure step (suggest Step 1.8 — sibling to IB-158's Step 1.7; or fold into Step 1.7's branching when form is `template` — executor's choice based on read-cohesion vs separation tradeoffs). For each template-classified finding (forms `rule` and `skill` continue routing through Step 1.7's DD-97 path; templates branch to this new path; agents branch to Item 2 below): perform an LLM-loose corpus scan over `extracts/templates/` for candidates the new finding is an evolution of (calibrated like DD-97 — false-positives caught at Nick's gate). On evolution match, emit a structured proposal — same shape as IB-158's extension-proposal report but targeted at `operations/version-bump-proposals/<YYYY-MM-DD>-version-bump-proposals.md` (or extension of `operations/extension-proposals/` with target form distinguishing rows — executor's choice; new directory cleaner for separation). Per-proposal block carries: existing template's current highest version (computed by enumerating `<name>.md` and `<name>-v*.md` siblings; max of the integers — implicit unsuffixed file is v1), candidate evolution's diff sketch (how the new finding's content evolves the existing template), proposed `<name>-v<N+1>.md` filename, Codifier recommendation from closed enum (`version-bump` | `create new (false positive)` | `defer`). On Nick ruling `version-bump`, `/extract-artifacts` writes `<name>-v<N+1>.md` with FULL INDEPENDENT FRONTMATTER per DD-100 §The Constraint: own `source_finding` (typically the version-bumping finding; can be different if Nick directs); own `extraction_date` (current write date; never inherited); own DD-95 `last_change_session` + `last_change_sl`; own DD-78 `contract` (ContractSpec); own DD-92 `context` (ContextSpec); own `version: <N+1>` field (per IB-161's schema addition). NEVER overwrite the existing `<name>.md` body or any existing `<name>-v<M>.md` body — versioning is side-file by contract. **Item 2 — Agent flag-only path (DD-82 invariant).** When the form is `agent`, DD-82's never-auto-create invariant fires first. The skill flags the agent-classified finding for Nick's review (e.g., emit a structured note in the run report citing the finding stem, the candidate's agent shape, and the DD-82 + DD-100 §Rules #4 prior-approval requirement) and EXITS WITHOUT DRAFTING — never writes a version-bump proposal, never writes a v2 file, never invokes any auto-bump path. If Nick subsequently approves a version bump as a separate manually-scoped action, `/extract-artifacts` is invoked with explicit version-bump intent (`--version-bump <name>` or equivalent flag, executor's choice of arg name) — same write mechanics as templates (full independent frontmatter, side-file write at `<agent>-v<N+1>.md`), but the prior approval is structural, not skill-resolvable from the corpus scan. **Item 3 — Compute-N + collision + missing-baseline rules.** Per DD-100 §Rules for `/extract-artifacts` items 5–7: (a) compute next N by enumerating `<name>.md` and `<name>-v*.md` siblings in the directory and taking `max(existing versions) + 1`; (b) pre-write filename collision check against `<name>-v<N>.md` aborts with structured error on collision (likely cause: N-computation drift or manual file creation); (c) abort with structured error if the unsuffixed `<name>.md` baseline is missing (deleted in error) — never silently re-create v1 from a non-v1 source; recovery is human review (restore v1 from git, then re-run); (d) once `<name>-v<N>.md` exists, NEVER overwrite it — subsequent regen produces `<name>-v<N+1>.md`, not in-place rewrite. **Item 4 — Procedure-table + DD-table updates.** Failure-modes table gains rows for: (i) write attempt when v1 baseline missing; (ii) filename collision on N+1; (iii) auto-bump attempt for agent form (procedural violation — DD-82 invariant); (iv) attempt to overwrite an existing `<name>-v<N>.md` body; (v) version-bump on a finding whose corpus scan finds NO match (skill must distinguish 'no match → proceed to draft new template at unsuffixed name' from 'match → proposal'). Design Decisions table gains DD-100. **Rules (subset of DD-100 §Rules for `/extract-artifacts`).** (1) Templates auto-scan; agents flag-and-exit (DD-82 invariant). (2) Nick gates the bump; no auto-write. (3) Full independent frontmatter on every version (DD-95, DD-78, DD-92, IB-161 schema field — all per-version). (4) Compute-N enumeration; collision-abort; missing-baseline-abort. (5) No in-place version overwrite. (6) DD-101 (IB-164) queue rows of target form `template` flow through THIS path on promotion — IB-164's queue-row consumer is upstream of IB-162's version-bump scan, not parallel to it. **Acceptance Criteria (subset of DD-100 §Acceptance Criteria).** (i) Template-classified finding is an evolution of `existing-template.md` (v1 implicit) — Codifier emits a version-bump proposal naming the existing template and the candidate evolution; no file written until Nick rules; (ii) Nick rules `version-bump existing`; `/extract-artifacts` runs — `existing-template-v2.md` created with independent frontmatter (`version: 2`, own `source_finding`, own `extraction_date`, own `last_change_*`, own `contract`, own `context`); `existing-template.md` body unchanged; (iii) Agent-classified finding evaluated by `/extract-artifacts` without Nick prior approval — skill flags for Nick review and exits; no version-bump proposal emitted; no file written; (iv) Nick approves an agent version bump; manual version-bump invocation runs — `<agent>-v2.md` created with independent frontmatter; original `<agent>.md` body unchanged; (v) Three versions exist (`<name>.md`, `<name>-v2.md`, `<name>-v3.md`); next regen — new file is `<name>-v4.md`; existing v1, v2, v3 unchanged; (vi) `<name>-v2.md` is regenerated in-place by skill bug — procedural violation, flagged in next governance audit; (vii) Template-classified finding has NO corpus-scan match — proceeds to draft new template at unsuffixed name (regression check; behavior matches pre-IB-162 baseline). **Dependencies.** **Blocked by IB-161** — the `version: <N+1>` frontmatter field requires the `_schema.yaml` schema definition from IB-161 to exist before IB-162's writes are spec-compliant. IB-158 (Step 1.7 corpus-scan + extension-proposal infrastructure for `/extract-artifacts` — IB-162 is the parallel template-form path and may share the corpus-scan implementation pattern). IB-156 (DD-95 `last_change_*` writer — every new version inherits IB-156's full lifecycle pointer machinery). DD-78 ContractSpec writer (already present in skill per IB-150). DD-92 ContextSpec writer (already present in skill per IB-150). **Touch Points.** `.claude/skills/extract-artifacts/SKILL.md` (new procedure step or branch off Step 1.7 + Step 2 amendment to filter version-bump-proposed findings out of new-template drafting + Step 3 frontmatter template extension to include `version` field for template/agent forms + Failure Modes additions + Design Decisions table addition); new directory `operations/version-bump-proposals/` if executor chooses separation (created on first proposal emission, not pre-created). NO touches to `extracts/templates/` or `extracts/agents/` from IB-162 directly — version-bumped files are written by the skill at run time, not by IB-162's procedure-edit work. **Form precedent.** IB-158 (Step 1.7 corpus-scan + extension-proposal report on `/extract-artifacts` — IB-162's structural twin for template form); IB-156 (writer-only update to `/extract-artifacts` with new procedure steps + arg additions)."
milestone: null
---
