---
name: "Implement /extract-artifacts template version-bump path + agent flag-only path (DD-100)"
id: "IB-162"
source_dd: "DD-100"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 76. **Item 1 — Template version-bump corpus scan.** Inserted new Step 1.8 in `.claude/skills/extract-artifacts/SKILL.md` between Step 1.7 (DD-97 rule/skill extension scan) and Step 2 (Draft Artifacts). Step 1.8 governs both template and agent forms via two named branches (A — templates auto-scan; B — agents flag-and-exit). Calibration matches DD-97's LLM-loose philosophy: no structured threshold; LLM judgment on title + summary + body + ContractSpec; false-positives caught at Nick's gate. Branch A enumerates `extracts/templates/<name>.md` and `<name>-v*.md` siblings, groups by baseline stem, scan-targets the highest-version member of each group (lower versions are historical). Per-finding categorization: no_match (proceed to Step 2 as new template), single_match (proposal emitted), multi_match (proposal with strongest match as primary + secondaries flagged). Aggregated proposals written to `operations/version-bump-proposals/<YYYY-MM-DD>-version-bump-proposals.md` (executor's choice resolved: separate directory, cleaner separation of DD-97 extension proposals vs DD-100 version-bump proposals). Per-proposal block carries: existing template stem + current version, secondary matches (omit if zero), proposed `<name>-v<N+1>.md` filename, Codifier recommendation from closed enum (`version-bump` | `create new (false positive)` | `defer`), why-this-match, structural diff sketch, optional notes. Report frontmatter type `version-bump-proposals-report`. **Item 2 — Agent flag-only path (DD-82 invariant).** Branch B handles agent-classified findings without `--version-bump`: never scan corpus, never emit proposal, never auto-bump. Emit a structured note in run report citing finding stem, candidate agent shape, DD-82 + DD-100 §Rules #4 prior-approval requirement, recommended next action (Nick reviews → re-invoke `/extract-artifacts --version-bump <stem>` if approved). Tag `version_bump_status: \"agent-flagged\"`; skip Step 2 drafting. Branch B' (`--version-bump <agent-stem>` set) bypasses flag-and-exit and routes the named candidate through versioned-write path; verifies baseline existence; enforces single-target rule (abort on >1 finding targeting the same stem). Tag `version_bump_status: \"agent-approved-bump\"`. **Item 3 — Compute-N + collision + missing-baseline rules (writer-side).** Codified in Step 1.8 Item 3 (a)–(e) and applied in Step 3's new versioned-write branch. (a) Enumerate siblings; N = max(existing versions) + 1; v1 implicit on unsuffixed baseline. (b) Pre-write filename collision abort. (c) Missing v1 baseline abort with restore-from-git recovery. (d) No in-place version overwrite — subsequent regen produces v<N+1>, never rewrites existing version. (e) Full independent frontmatter on every version — own `source_finding`, own `extraction_date` (current write date), own `last_change_*` (DD-95), own `contract` (DD-78), own `context` (DD-92), own `version: <N>` (DD-100 + IB-161 schema field). `--version-bump` + `--update` ill-formed combination → abort. **Item 4 — Step 2 filter + Step 3 versioned-write branch + frontmatter template + failure-modes + DD-table.** Step 2 filter amended to exclude `version_bump_status: \"proposed\"` AND `version_bump_status: \"agent-flagged\"` from drafting (parallel to existing `extension_status: \"proposed\"` exclusion); `version_bump_status: \"no_match\"` proceeds normally; `version_bump_status: \"agent-approved-bump\"` proceeds to drafting AND routes through Step 3's versioned-write path. Step 3 dedup section gains a third bullet: 'Versioned-write branch (DD-100)' citing Step 1.8 Item 3 rules and aborting on `--update`+versioned-write combination. Step 3 frontmatter template gains `version:` line between `last_change_sl:` and `identification_report:` (matches IB-161 backfill placement convention) with comment 'required for template/agent v2+; optional v1; omit for rule/skill/pattern/guide'. Failure-modes table gains 8 new rows: missing-v1-baseline; filename-collision; auto-bump-attempt-for-agent; in-place-overwrite; no-match-pass-through (regression-equivalent disambiguator); --version-bump-on-template; --version-bump multi-target; --version-bump+--update combo. Design Decisions table gains DD-100 (full row citing Step 1.8 + Step 2 filter + Step 3 versioned-write branch + frontmatter `version` field) AND DD-82 (three-layer enforcement: flag-and-exit + explicit operator scoping + form-mismatch guard). New `--version-bump <agent-stem>` argument added to Arguments table with full semantics. New `operations/version-bump-proposals/` path added to Paths table. **Acceptance addressed.** (i) Template-classified evolution → version-bump proposal emitted; no file written until Nick rules — Branch A flow. (ii) Nick rules version-bump → `existing-template-v2.md` written with full independent frontmatter; v1 body unchanged — Step 3 versioned-write branch + Item 3 (d). (iii) Agent-classified without prior approval → flag-and-exit; no proposal, no write — Branch B. (iv) Nick approves agent bump → manual `--version-bump <stem>` invocation runs; `<agent>-v2.md` written with independent frontmatter; v1 unchanged — Branch B'. (v) Three versions exist → next regen produces v4; v1/v2/v3 unchanged — Item 3 (a) + (d). (vi) In-place v2 regen by skill bug → procedural violation flagged in governance audit — Item 3 (d) + failure-modes row. (vii) Template no-match → proceed to draft new template at unsuffixed name; regression-equivalent — `version_bump_status: \"no_match\"` Step 2 path + failure-modes disambiguator row. **Executor's-choice resolutions.** (1) Separate Step 1.8 (sibling to Step 1.7) over folding template branch into Step 1.7 — cleaner read-cohesion: Step 1.7 owns DD-97/rule+skill, Step 1.8 owns DD-100/template+agent; per-step DD ownership maps cleanly. (2) `operations/version-bump-proposals/` separate directory — chosen for separation of DD-97 vs DD-100 proposal flows; future cross-form folding (e.g., 'extension-or-version-proposals') can be a follow-up DD if both directories accumulate cross-references. (3) Argument naming `--version-bump <agent-stem>` (single-stem positional argument) — matches the IB suggestion; minimal surface; clear semantics; rejects multi-target invocations explicitly. **Bugs surfaced.** None. **Contract amendments proposed/applied.** None. **Logged for future.** (a) IB-164's template-target dispatch will route queue rows through this Step 1.8 — when a queue row's target form is `template`, IB-164's `/extract-artifacts` consumer mode treats the row's source excerpt as a template-classified finding and runs Step 1.8 Branch A (auto-scan + propose). The propose-then-Nick-rules flow is shared between identification-report templates and harvest-queue-row templates. (b) DD-100's '~12 templates and ~5 agents' estimate is now superseded by IB-161's verified 5 + 2 (session-76 execution). The Step 1.8 corpus-scan target count is exact at execution-time; no contract dependency on the estimated counts. (c) The disposition table (template/agent × `--version-bump`) is documented in Step 1.8 body for operator reference; if confusion surfaces, promote to an Arguments-table example. (d) Versioned writes inherit Step 2.7's session/SL resolution unchanged — the write loop reuses the cached `(active_session, active_sl_stem)` pair regardless of new vs versioned. No new Step 2.7 amendment needed. (e) Future DD may want to scope `version: <N>` field's frontmatter placement convention into the schema comment block (IB-161 schema block currently says 'matches integer suffix'; could add 'placement: after `last_change_sl`, before `identification_report` per session-76 convention'). Not blocking; low value over current."
milestone: null
---
