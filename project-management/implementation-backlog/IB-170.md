---
name: "Rationalize concept docs, agent reference/helper files, and the knowledge/ folder"
id: "IB-170"
source_dd:
  - "DD-111"
  - "DD-112"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
notes: "Nick (2026-06-19) flagged a structural question he's been mulling: where do concept docs, agent/persona reference + helper files, and knowledge/ content belong, and how do they relate? Concrete trigger surfaced during the DD-110 framing reconciliation (session 123): concept docs are split across two homes — per-artifact concept docs (skill.md, agent.md, prompt.md, memory.md) live in operations/references/librarian/, while harness.md lives in knowledge/reference/. That inconsistency is the immediate symptom; the broader question is the relationship between (a) Librarian concept docs (the middle-altitude configuration axes), (b) agent/persona reference + helper files under agents/, and (c) the knowledge/ folder (patterns, guides, templates, reference, schematics). Discuss-first: this is a content/architecture decision (placement + ownership taxonomy), not a mechanical move. Scope a deliberate spec before moving anything. Do NOT relocate files ad hoc."
milestone: null
type: "Decision"
---

# IB-170: Rationalize concept docs, agent reference/helper files, and the knowledge/ folder

A structural-organization question Nick flagged on 2026-06-19. Not a mechanical cleanup — a deliberate placement-and-ownership decision to be specced before any files move.

## The immediate symptom

During the DD-110 post-collapse framing reconciliation (session 123), concept docs were found split across two homes:

- Per-artifact concept docs — `skill.md`, `agent.md`, `prompt.md`, `memory.md` — live in `operations/references/librarian/`.
- The harness concept doc — `harness.md` — lives in `knowledge/reference/`.

Same class of artifact (Librarian concept docs / configuration axes), two different homes.

## The broader question

How should these three things be organized and how do they relate?

1. **Concept docs** — the Librarian's middle-altitude configuration axes (skill / agent / prompt / harness / memory), each with §Composition + §Construction.
2. **Agent / persona reference + helper files** — material under `agents/` that supports the Owner / Researcher / Codifier / Librarian dispositions.
3. **The `knowledge/` folder** — `patterns/`, `guides/`, `templates/`, `reference/`, `schematics/`.

Open sub-questions: Is `operations/references/` the right home for concept docs, or should they live in `knowledge/reference/`? What distinguishes "operational reference" from "knowledge reference"? Where do agent-specific helper files sit vs. shared knowledge? Does the three-altitude model (DD-104) suggest a cleaner placement taxonomy?

## How to approach

- **Discuss-first.** Content/architecture decision; Nick gates the taxonomy.
- **Spec before build** (Charter value). Produce a placement/ownership spec; only then move files (with `git mv`, updating all pointers).
- Rule 11: don't manufacture a taxonomy for symmetry — let the concrete inconsistencies (e.g., the concept-doc split) drive the minimum viable reorganization.

## Session 124 update (2026-06-20)

Nick reframed and broadened the task. The concept-doc split is now understood as a *sub-case* of a
larger confusion: `extracts/` (research substrate the Librarian composes) vs. `knowledge/` (the
engine's self-knowledge) are two different bodies sharing folder names, with the DD-80 "staging"
label no longer describing what `extracts/` is.

Done this session:
- **Archived** the pre-collapse `household-os/` reference subtree → `archive/household-os/` (retained
  as design substrate for the planned Notion second-brain work; 2 live schematic pointers updated).
- **Fixed drift** in `knowledge/guides/research-to-codification-pipeline.md` (stale form-rubric path;
  Current State; `_index` upkeep step).
- **Slimmed** `knowledge/guides/skill-authoring-guide.md` to a SKILL.md mechanics reference (design
  content superseded by `librarian/skill.md` §Construction + `/design-skill`).
- **Produced the gate input:** `project-management/design-notes/2026-06-20-extracts-knowledge-reconciliation.md`
  — live-vs-orphaned audit (guides 100% live; patterns Tier-2; rules/skills/templates/agents = staging
  residue) + recommended model (recognize substrate vs. staging; promote-or-prune the residue).

Next gate: Nick decides the substrate framing (rename-in-place vs. relocate) and the staging-residue
disposition. The concept-doc placement question (original symptom) follows that decision.

## Session 126 update (2026-06-21) — knowledge-architecture sweep

The upstream gates are resolved, unblocking the concept-doc question:
- **Substrate framing settled:** Nick gated **1a (rename-in-place)** → **DD-111** filed (`extracts/guides`
  + `extracts/patterns` recognized as Librarian substrate, not staging; `extracts/CLAUDE.md` + the
  pipeline guide reframed).
- **Staging residue dispositioned:** the four residue subtrees (`rules/skills/templates/agents`, ~121
  files) designated an explicit **harvest archive** (distilled external best-practice, not engine
  artifacts; kept for `/synthesize-guide` input; promotion is per-item Nick-gated, DD-29). No files
  moved or deleted. (DD-111.)

**Concept-doc placement spec produced** (this item's core):
`project-management/design-notes/2026-06-21-ib170-concept-doc-placement.md`. Finding: the real
inconsistency is narrow — every middle-altitude concept doc unifies §Composition+§Construction in one
file under `operations/references/librarian/`; the **harness concept alone is split across two homes**
(`knowledge/reference/harness.md` §Construction vs. `librarian/harness.md` §Composition), and the two
files describe two distinct concepts sharing the name "harness" (whole-system unit vs.
runtime-environment axis). Recommended taxonomy (gated): home rule (`knowledge/reference/` = engine
self-knowledge; `operations/references/` = operational reference incl. all concept docs); consolidate
`knowledge/reference/harness.md` into `operations/references/librarian/`; rename the runtime-env axis →
`runtime-environment.md`, keep `harness.md` for the whole-system unit.

**Executed (Nick gated "Adopt + execute now") → DD-112 filed.** Concept-doc home rule adopted
(`knowledge/reference/` = engine self-knowledge; `operations/references/` = operational reference incl.
all concept docs). `knowledge/reference/harness.md` relocated → `operations/references/librarian/harness.md`
(whole-system unit); the runtime-sense doc renamed → `operations/references/librarian/runtime-environment.md`;
all live pointers repointed (`consumer-abstractions-map.md` ×4, both `_index.md` files, internal §Composition
pointers). `knowledge/reference/` now holds only self-knowledge. Agent-helper-file question: no change
(Rule 11). **IB-170 resolved** — all three sub-questions (concept docs, agent helper files, knowledge/
relationship) addressed across DD-111 (two bodies) + DD-112 (concept-doc placement).

## Provenance

Flagged by Nick (2026-06-19) at session 123 close, building on the concept-doc-split observation surfaced while reconciling DD-110's post-collapse framing follow-up.
