---
name: "Update /extract-artifacts skill to generate ContextSpec per DD-92"
id: "IB-150"
source_dd: "DD-92"
status: "Queued"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Update the /extract-artifacts skill at `.claude/skills/extract-artifacts/SKILL.md` to generate ContextSpec by default on every extracted artifact. (1) Add ContextSpec fields to the subagent prompt template and the artifact write format (applies_to, platform_coupling, autonomy, stage, reversibility, auditability, evidence_strength, adoption.status, adoption.notes). (2) Enforce universal-vocabulary constraint — emit a flag if `applies_to` mechanically copies source finding `applicability` rather than being re-derived in universal vocabulary. (3) Strip IL classification meta (confidence, tier, reason_codes, co_occurrence) from the artifact — these are IL-internal and must not travel across the deploy boundary. (4) Refuse to write an artifact missing ContextSpec, same enforcement as DD-78's missing-ContractSpec rule. Acceptance: one dry run over a new finding produces a DD-92-conformant artifact without manual post-edits. Reference implementation: `extracts/rules/confirm-failure-first-tdd.md`."
milestone: null
---
