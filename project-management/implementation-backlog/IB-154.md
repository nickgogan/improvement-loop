---
name: "Implement /synthesize-guide preserved-section enforcement (DD-93)"
id: "IB-154"
source_dd: "DD-93"
status: "Open"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Codifier scope. Update `.claude/skills/synthesize-guide/SKILL.md` to implement DD-93's two-surface preservation contract: (1) the canonical `## Nick's Annotations` section, and (2) any block bounded by `<!-- PRESERVE -->` … `<!-- /PRESERVE -->` HTML comments. Procedure: pre-regen — parse the existing guide and capture (a) the body of `## Nick's Annotations` if present, and (b) the body of every `<!-- PRESERVE -->` … `<!-- /PRESERVE -->` region in document order. Regen — generate the structural body from `source_findings[]` per existing logic, then re-insert preserved content at corresponding positions (named section at original ordinal location or appended; marked regions at closest semantically-equivalent location). Post-regen regression test — re-extract preserved surfaces from candidate output, byte-compare against captured pre-regen content, abort write on any drift with structured drift report (which surface, offending diff, recommended fix). Acceptance: 4 cases from DD-93 §Acceptance Criteria — guide with named section preserved verbatim across regen; guide with mid-Pitfalls marked region preserved verbatim with surrounding content fully regenerated; guide with multiple marked regions all preserved in document order; guide with no preserved surfaces behaves identically to current full-regenerate (no-op). Plus negative case: simulated skill bug causing preserved-section drift → write aborts, no file change committed. No deploy from this IB — `/synthesize-guide` is consumed at next G7/G2/G9 re-synthesis. Phase-1 lifecycle bundle (alongside IB-155, IB-156)."
milestone: null
---
