---
name: "Implement /synthesize-guide preserved-section enforcement (DD-93)"
id: "IB-154"
source_dd: "DD-93"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 71. Applied three procedure-step inserts to `.claude/skills/synthesize-guide/SKILL.md`: (1) Step 0.5 — Pre-Regen Preservation Capture (re-synthesis only) — captures `## Nick's Annotations` body (level-2 ordinal-tracked) and every `<!-- PRESERVE -->` … `<!-- /PRESERVE -->` region in document order with anchor-section + ordinal-in-section metadata; validates marker structure (matched, non-nested) and aborts on bad markers; (2) Step 3.5 — Re-Insert Preserved Content — replaces or appends the named section at original ordinal location (with tail fallback if structural template no longer reserves a slot), re-inserts marked regions at the closest semantically-equivalent location under the captured anchor section (with documented tail-fallback wrapped in markers + HTML comment when the anchor disappears); (3) Step 3.7 — Post-Regen Regression Test (fail-closed) — re-extracts preserved surfaces from candidate body, byte-compares against pre-regen capture across annotations and every region in document order, aborts the write on any inequality with a structured drift report (which surface, unified diff, recommended fix path) and exits non-zero. Failure-modes table gains two rows (unmatched/nested markers; preserved-section drift). Design Decisions table gains DD-93. No-op for guides with neither a named section nor marked regions — legacy full-regenerate path unchanged. All 4 acceptance cases from DD-93 §Acceptance Criteria + 1 negative (skill bug → drift → write aborts) addressed by procedure design. Live validation deferred to next-session G7 / G2 / G9 re-synthesis (the real acceptance gate). Bugs surfaced: none. Contract amendments proposed: none. Logged for future: ordinal-position heuristic may need refinement if a future guide grows level-3+ structure that the structural template emits at different positions — surface only if a real re-synthesis trips on it. Phase-1 lifecycle bundle (alongside IB-155, IB-156)."
milestone: null
---
