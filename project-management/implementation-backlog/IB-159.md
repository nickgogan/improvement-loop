---
name: "Implement /synthesize-guide + /identify-artifacts split-trigger detection (DD-98) + DD-94 enum bullet add for `guide-split`"
id: "IB-159"
source_dd: "DD-98"
status: "Queued"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Phase-3 lifecycle bundle (alongside IB-160, IB-161, IB-162, IB-163, IB-164). Implements DD-98's split-trigger detection at both natural read points per DD-98 §Detection: (a) `/synthesize-guide` Step 0 routing-table read for cluster resolution; (b) `/identify-artifacts` routing-table read for unrouted-bucket review (the same Step 6 IB-160 also amends — coordinate to avoid edit collision). **Scope.** Three work items, all required for DD-98 to be operative on the next regen / identification cycle. **Item 1 — `/synthesize-guide` detection.** Add detection logic to Step 0 (routing-table read): for the active guide cluster, evaluate the trigger conjunction (count ≥25 AND practitioner-question count ≥2 in cluster findings). On match, emit a structured proposal at `operations/split-proposals/<YYYY-MM-DD>-<guide-stem>-split-proposal.md` per DD-98 §Response shape — source identity, practitioner-question analysis, proposed bifurcation (per-finding routing: A | B | shared | contested), preserved-section disposition (every `## Nick's Annotations` block AND every `<!-- PRESERVE -->` region under DD-93 invariant — explicit recommendation per region: route-to-A | route-to-B | duplicate-to-both with rationale), routing-table impact (deprecation + new rows), Codifier recommendation (`proceed with split as proposed` | `defer pending more findings` | `re-evaluate practitioner-question bifurcation` | `absorb into adjacent guide instead`). Read-only by contract: never auto-execute, never write destination guide files, never update routing table, never deprecate source guide, never modify DD-94 changelog files. Single-condition observations (count crosses but practitioner-question stays at 1, or vice versa) are surfaced inline in the synthesis report as informational ('G-N at <count> findings; remains single-question') but do NOT emit a proposal file. **Item 2 — `/identify-artifacts` detection.** Mirror Item 1's trigger evaluation when the skill reads the routing table. On match, surface a heads-up in the identification report and emit the same split-proposal artifact at `operations/split-proposals/`. Both detection paths share identical emission semantics — the artifact path and shape are skill-agnostic. **Item 3 — DD-94 enum bullet edit.** Add `guide-split` as a closed-enum trigger tag in DD-94's §The Constraint enum bullet list (current enum: `staleness-threshold` | `nick-request` | `dimension-rebalance` | `finding-removed` | `structural-edit` per IB-155). The new tag is used in two contexts per DD-98 §Trigger-Tag Enum Amendment: source guide's final entry ('Split into destinations [[<dest-A>]] and [[<dest-B>]] per [[DD-XXX]]'); destination guide's first entry ('Derived from source [[<source>]] per [[DD-XXX]]'). The `/synthesize-guide` Step 4.5 appender's enum check (added in IB-155) must be updated to accept `guide-split` in addition to the current 5 tags. Cross-IB note: IB-160 also adds an enum entry (`theme-graduation`) to the same DD-94 enum bullet list; the two edits are additive and non-conflicting. Each IB owns its own bullet addition; IB-159 adds `guide-split` and IB-160 adds `theme-graduation` independently. **Rules (subset of DD-98 §Rules for Codifier).** (1) Detect on natural read points only — no separate sweep skill. (2) Both thresholds required; single-condition is observation, not trigger. (3) Propose via report file at `operations/split-proposals/`; never modify source guide frontmatter. (4) Bifurcation precision — every finding routed (A | B | shared | contested); proposal without disposition is a defect. (5) Preserved-section disposition is explicit per region; implicit handling forbidden per DD-93 invariant. (6) Never auto-execute; execution requires Nick-approved per-split DD. **Acceptance Criteria (subset of DD-98 §Acceptance Criteria).** (i) Guide reaches ≥25 findings with single coherent practitioner question — NO proposal emitted; routing-table observation may be surfaced inline; (ii) Guide reaches ≥25 findings AND covers ≥2 distinct practitioner questions — proposal file written with per-finding bifurcation and preserved-section disposition; (iii) Codifier emits split proposal that auto-executes — procedural violation, flagged in next governance audit; (iv) Source guide has preserved sections; proposal must specify disposition per region (zero implicit handling); (v) After IB-159 lands, `/synthesize-guide` Step 4.5 appender accepts `guide-split` as valid trigger tag (and rejects unknown tags as before per IB-155); (vi) DD-94's §The Constraint enum bullet list shows `guide-split` as a valid entry. **Dependencies.** IB-155 (DD-94 enum infrastructure exists in the appender to extend); IB-154 (DD-93 preserved-section capture exists; needed for proposal's preserved-section disposition section to be machine-emittable). No blocking dependency on IB-160 (each IB executes its own DD-94 bullet edit independently). **Touch Points.** `.claude/skills/synthesize-guide/SKILL.md` (Step 0 detection insert + Step 4.5 enum extension); `.claude/skills/identify-artifacts/SKILL.md` (routing-table-read detection insert); `project-management/design-decisions/DD-94.md` (single bullet add to §The Constraint enum list); new directory `operations/split-proposals/` created on first proposal emission (not pre-created). **Form precedent.** IB-154 (procedure-step inserts to `/synthesize-guide`); IB-155 (multi-work-item structure with three required items)."
milestone: null
---
