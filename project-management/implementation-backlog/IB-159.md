---
name: "Implement /synthesize-guide + /identify-artifacts split-trigger detection (DD-98) + DD-94 enum bullet add for `guide-split`"
id: "IB-159"
source_dd:
  - "DD-98"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 76. **Item 1 — `/synthesize-guide` detection (Step 0.7).** Inserted new Step 0.7 (Split-Trigger Detection) between Step 0.5 (preservation capture) and Step 1 (Read and Analyze Findings). Step 0.7 evaluates the DD-98 conjunction (count ≥25 AND practitioner-question count ≥2) against the resolved finding set + routing-table cluster identity from Step 0. On both-thresholds match: emits structured split-proposal artifact at `operations/split-proposals/<YYYY-MM-DD>-<guide-stem>-split-proposal.md` with full per-DD-98-§Response shape (source guide identity; practitioner-question analysis; proposed bifurcation with per-finding routing table — A | B | shared | contested; preserved-section disposition per region per DD-93 invariant; routing-table impact; Codifier recommendation from closed enum). On single-condition match: NO file emitted; inline informational note in synthesis report (count-only or question-only specific wording per dispatch table). On neither: no-op. Initial-synthesis is no-op (trigger evaluates against existing cluster mass + question bifurcation; brand-new guide has neither). Read-only by contract: never auto-execute; never write destinations; never update routing table; never deprecate source; never modify DD-94 changelog files based on its own proposal. Filename collision append `-2`/`-3` if two regens collide on same day. **Item 2 — `/identify-artifacts` detection (Step 6.a).** Step 6 amended to add Step 6.a inline (mirrors Step 0.7 trigger evaluation; identical emission semantics — same proposal artifact path and shape). Both detection paths share the proposal artifact format; the path and shape are skill-agnostic. If both skills detect on the same trigger in the same cycle, both emissions are written (filename collision triggers `-2`/`-3` suffix). Step 6 dispatch table mirrors Step 0.7's. Identification-report summary block gains: heads-up line on split-trigger fires + cluster list + proposal pointer; inline single-condition observations from dispatch table when applicable. Coordinated-edit posture: Step 6 is also amended by IB-160 (graduation-trigger detection on Unrouted Bucket review); both edits are additive — IB-159 adds Step 6.a; IB-160 will add Step 6.b. No collision because the work items target different routing-table surfaces (Step 6.a = Active Clusters per-cluster trigger; Step 6.b = Unrouted Bucket cluster review). **Item 3 — DD-94 enum bullet add (`guide-split`).** Edited DD-94 §The Constraint enum bullet list to add the `guide-split` bullet after `structural-edit`. Bullet body documents the two-context use per DD-98 §Trigger-Tag Enum Amendment: source guide's final entry ('Split into destinations [[<dest-A>]] and [[<dest-B>]] per [[DD-XXX]]') and destination guide's first entry ('Derived from source [[<source>]] per [[DD-XXX]]'); cites DD-98 (Phase-3 ratification) and IB-159 (implementation, session 76). `/synthesize-guide` Step 4.5 appender's enum check updated to accept `guide-split` in addition to the prior 5 tags (closed enum now: `staleness-threshold`, `nick-request`, `dimension-rebalance`, `finding-removed`, `structural-edit`, `guide-split`). **Cross-IB enum sequencing executor's-choice resolution.** IB-160 (parallel) also amends DD-94 enum and Step 4.5 — to add `theme-graduation`. Resolution: **landed additively across IB-159 and IB-160 commits** (each IB owns its own bullet add; Step 4.5 enum extension is folded into each IB's commit). IB-159 ships with `guide-split` only in both surfaces; IB-160 will ship `theme-graduation` separately. The decision honors per-IB-atomic-commit cleanliness — each IB-160 commit can be reverted independently if the corresponding DD is amended, without touching IB-159's commit. Alternative (consolidate-in-IB-159) was rejected because it muddles per-IB attribution in commit history and complicates revert paths. **Acceptance addressed.** (i) Guide reaches ≥25 findings with single coherent practitioner question → NO proposal emitted; routing-table inline observation (count-only path) — Step 0.7 + Step 6.a dispatch table. (ii) Guide reaches ≥25 AND ≥2 distinct questions → proposal file written with per-finding bifurcation + per-region preservation disposition — Step 0.7 + Step 6.a both-thresholds branch. (iii) Codifier auto-executes split → procedural violation: Step 0.7 + Step 6.a are read-only by contract; never write destinations or update routing table; flagged in next governance audit if attempted. (iv) Source guide has preserved sections; proposal must specify per-region disposition — Step 0.7 proposal shape requires `**Preserved-section disposition (DD-93)**` block with one entry per captured region; implicit handling forbidden. (v) After IB-159 lands, Step 4.5 appender accepts `guide-split` as valid trigger tag — confirmed in enum-validation line. (vi) DD-94's §The Constraint enum bullet list shows `guide-split` — confirmed in DD-94 edit. **Executor's-choice resolutions.** (1) Step number for synthesize-guide split-detection: Step 0.7 (between Step 0.5 preservation capture and Step 1 finding read; reasoning: needs cluster identity + finding count from Step 0; needs preservation captures from Step 0.5 for the proposal's preserved-section disposition section to be machine-emittable). (2) Identify-artifacts step number: Step 6.a (additive within Step 6's existing routing-table-read flow; mirrors Step 0.7 emission semantics). (3) DD-94 enum sequencing: additive across IB-159 / IB-160 commits (cleaner attribution; safer revert paths). **Bugs surfaced.** None. **Contract amendments proposed/applied.** Edits to DD-94 body are not amendments per DD-44 — DD-98 IS the DD amendment for the new tag (per DD-98 §Trigger-Tag Enum Amendment to DD-94); IB-159 is the mechanical execution of DD-98's amendment via the bullet edit. No DD frontmatter `updated:` field touched on DD-94 (the enum bullet list is the only change; matches the additive-edit posture documented in DD-98). **Logged for future.** (a) Step 0.7's initial-synthesis-no-op posture is defensive — the trigger semantics still apply if a future curated finding set hits both thresholds on first synthesis; the failure-modes table documents this edge case. Surface only if observed. (b) Step 6.a re-uses the same proposal-artifact shape as Step 0.7 — if both fire in the same cycle, both write proposal files (filename collision on same date triggers `-2`/`-3`). The cross-skill emission count is informational; future polish may consolidate via a shared emission helper, but premature for current volume. (c) Codifier-recommendation field is closed-enum (`proceed` | `defer` | `re-evaluate` | `absorb`); the proposal file shape documents this. If the corpus exercises the path and Nick observes recommendation drift, surface for closed-enum tightening. (d) Split-proposal artifacts will accumulate in `operations/split-proposals/`; no archival policy codified yet (parallel to DD-100's pruning-threshold open question). Defer until volume justifies a rule. (e) DD-94 enum is now 6 entries deep; the closed-vocabulary cost is acceptable but should be re-evaluated if Phase-N adds further entries beyond `theme-graduation` (IB-160). Not urgent."
milestone: null
---
