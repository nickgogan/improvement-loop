---
name: "Implement /extract-artifacts last_change_* writer + SL stem validation (DD-95)"
id: "IB-156"
source_dd: "DD-95"
status: "Open"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Codifier scope. Reduced scope vs original Phase-1 framing: the 31-artifact retroactive backfill across `extracts/{rules,skills,templates,agents}/` was executed in session 70 alongside DD-95 filing (per Nick directive), so this IB covers only the writer-side code change going forward. Update `.claude/skills/extract-artifacts/SKILL.md` to: (1) populate `last_change_session` (integer) and `last_change_sl` (SL filename stem, no `.md` extension) on every non-guide artifact create AND every update — drift regen, Nick-requested re-extraction, dimension rebalance; (2) overwrite both fields on update (do not preserve prior values in frontmatter — SL chain carries the history); (3) before writing, validate `operations/system-log/<last_change_sl>.md` exists; missing → write-abort with structured error naming the missing SL file; (4) abort the write if the active session number is unknown to the skill. Insertion point in artifact frontmatter: immediately after `extraction_date:` (matches the in-session backfill convention so old-and-new artifacts share the same field ordinal position). Guides are explicitly excluded — the writer must not add either field to anything in `extracts/guides/`. Acceptance: 6 cases from DD-95 §Acceptance Criteria — new non-guide extract carries both fields with valid types; update overwrites both with current session values; non-existent SL stem aborts write; guides do not receive the fields; `_schema.yaml` already updated in session 70 (verify on commit); session 70 backfill verified 31/31 artifacts. Phase-1 lifecycle bundle (alongside IB-154, IB-155)."
milestone: null
---
