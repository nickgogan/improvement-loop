---
name: "Implement /extract-artifacts last_change_* writer + SL stem validation (DD-95)"
id: "IB-156"
source_dd: "DD-95"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 71 (Phase B scope expansion). Applied to `.claude/skills/extract-artifacts/SKILL.md`: (1) three new arguments — `--session NN` (active IL session integer), `--sl STEM` (active session SL filename stem; bare stem only — skill strips path/extension if user passes a filename), `--update` (re-extraction mode that flips default dedup-skip to overwrite-existing for the DD-95 re-extraction path: drift-report ruling, Nick request, dimension rebalance); (2) new Step 2.7 `Resolve Lifecycle Pointer (DD-95) — non-guide artifacts only` between Step 2.5 (Validate Drafts) and Step 3 (Write Artifacts) — resolves both fields with prompt-on-missing (unless `--auto`, which aborts), validates `operations/system-log/<stem>.md` exists with structured-error abort on missing, caches `(active_session, active_sl_stem)` for the per-artifact write loop; (3) Step 3 `Check for filename collision and dedup behavior` rewritten to describe both default-mode (skip on existing source_finding) and update-mode (overwrite — body + ContractSpec + ContextSpec regenerated, `extraction_date` + `deployed` + `deployed_to` preserved, `last_change_session` + `last_change_sl` overwritten with current session values; filename unchanged); (4) Step 3 frontmatter template gains `last_change_session` + `last_change_sl` between `extraction_date` and `identification_report` — matches session-70 31-artifact backfill ordering; (5) Rule #4 (Dedup at write time) amended to describe update-mode behavior; (6) Failure-modes table gains four rows (missing `--session`, missing `--sl`, `--sl` resolves to non-existent SL file, `--update` mode targets a finding with no existing artifact); (7) Design Decisions table gains DD-95. All 6 acceptance cases from DD-95 §Acceptance Criteria addressed by procedure design. Resolved ambiguity (documented in session-71 SL Phase B): the IB asked for write-on-update support but the pre-existing skill is structurally create-only with dedup-skip — added `--update` flag flipping dedup-skip to overwrite-existing with explicit preserve/overwrite field rules. Live validation deferred to next non-guide artifact write. Bugs surfaced: none. Amendments proposed: none. Logged for future: none specific to this IB. Phase-1 lifecycle bundle (alongside IB-154, IB-155). The session-70 31-artifact backfill established the field ordering this writer follows; no further backfill required."
milestone: null
---
