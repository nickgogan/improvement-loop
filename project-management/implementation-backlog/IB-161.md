---
name: "Schema add `version: integer` for template/agent extracts (DD-100) + retroactive `version: 1` backfill"
id: "IB-161"
source_dd:
  - "DD-100"
status: "Done"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Closed session 76. **Item 1 — `_schema.yaml` schema field.** Added new sibling block `# === Versioning (template/agent extracts only — DD-100) ===` immediately after the DD-95 Lifecycle Tracking block (executor's-choice resolved: new sibling block over annotating field inside DD-95 block — IB's recommended placement; cleaner separation of DD-95 vs. DD-100 concerns). Block documents: integer-only constraint (no decimals, no semver, no minor versions); explicit-version suffixes start at -v2; filename-suffix correspondence (<name>-v2.md → version: 2); v1-implicit convention (unsuffixed file is v1; absence of field treated as version: 1); scope (template/agent ONLY, rules/skills excluded per DD-97 path, guides/findings/sources/authorities excluded); requirement (optional v1, required v2+); maintainer (`/extract-artifacts` per DD-100). Field comment cites optional-v1 / required-v2+ rule and filename-suffix correspondence per IB notes. **Item 2 — Retroactive backfill.** Backfilled `version: 1` to all extant template and agent artifacts. **Discrepancy from filing-time count:** session-75 SL recorded '6 templates and 3 agents'; actual filesystem at session-76 execution shows **5 templates** (`execution-context-profiles-mode-switching.md`, `one-shot-prd-prompt-for-system-bootstrap.md`, `task-to-file-routing-table-in-context-files.md`, `tech-stack-pinning-table.md`, `yaml-templates-with-embedded-elicitation.md`) and **2 agents** (`initializer-agent-scaffolding.md`, `qa-agent-independent-compliance-review.md`) — total 7 files, not 9. No `_index.md` present in either directory. Filing-time count appears to have been a session-75 miscount (session 75 close commit was 4b4bc23; no template/agent additions between filing and execution per `git log` on these directories). The IB's spirit — backfill ALL extant template/agent extracts — is satisfied by the 7-file backfill; acceptance criteria (ii) + (iii) are reframed at execution as 'all 5 templates + all 2 agents carry version: 1 post-backfill' with no behavioral difference. **Field placement:** placed `version: 1` immediately after `last_change_sl:` and before `identification_report:` (executor's choice within IB notes' allowance: 'after `extraction_date` or after the DD-95 `last_change_*` pair') — keeps DD-95 lifecycle pair contiguous and DD-100 version field as its logical sibling. **Acceptance addressed.** (i) `_schema.yaml` documents `version` for template+agent; required v2+, optional v1; other extract types unchanged — confirmed by file diff. (ii) All 5 templates carry `version: 1` post-backfill — confirmed by per-file edits. (iii) All 2 agents carry `version: 1` post-backfill — confirmed by per-file edits. (iv) No new `<name>-v2.md` files created by backfill — confirmed (frontmatter-edit only). (v) Schema documentation comments cover integer-only, optional-v1, required-v2+, filename-suffix correspondence, scope (template/agent only) — confirmed in new block body. **Executor's-choice resolutions.** Schema-block placement: new sibling block (cleaner). Field placement within frontmatter: after `last_change_sl`, before `identification_report` (adjacent to DD-95 lifecycle fields). **Bugs surfaced.** Filing-time count discrepancy (6+3 claimed vs 5+2 actual). Cause: session-75 SL miscount; not a session-76 deviation. No corrective action against extracts/ — the backfill captured what is present. Session-76 SL records the correction and propagates it for any downstream skill that reads from session-75 SL. **Contract amendments proposed/applied.** None. **Logged for future.** (a) Session-75 SL contains the miscount in its `capture_note` field ('extracts/templates/ (6 files) and extracts/agents/ (3 files)'); not amended in-session per DD-86 'no mid-session PROGRESS.md edits' precedent applied analogously to SL — session-76 SL carries the correction inline, which is the audit-trail-preserving alternative to in-place SL amendment. (b) DD-100 §Backfill paragraph cites '~12 templates and ~5 agents'; both that estimate and session-75's '6+3' are now superseded by session-76's verified 5+2; future amendment of DD-100 prose may want to incorporate but is not blocking. (c) `version: 1` placement convention (after `last_change_sl`, before `identification_report`) is established by this backfill; IB-162's writer-side write-template should match this placement when emitting v2+ frontmatter to keep human-readable consistency across versions."
milestone: null
---
