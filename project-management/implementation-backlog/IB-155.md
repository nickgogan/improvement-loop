---
name: "Implement guide companion-changelog appender + retroactive stubs (DD-94)"
id: "IB-155"
source_dd: "DD-94"
status: "Open"
target_system: "improvement-loop"
priority: "P2"
type: "Build"
notes: "Codifier scope. Two work items, both required to land DD-94 operationally. **Item 1 — `/synthesize-guide` appender.** Update `.claude/skills/synthesize-guide/SKILL.md` to write a companion changelog entry on every re-synthesis. Procedure: locate or create `extracts/guides/changelog/<guide-stem>.changelog.md` (file already exists or skill creates with title heading); construct entry per DD-94 §The Constraint shape (one-line header `## YYYY-MM-DD — Session NN — <trigger-tag>` + bullets for Findings/Added/Removed/Structural/Preserved/SL); enforce trigger-tag closed enum (`staleness-threshold`, `nick-request`, `dimension-rebalance`, `finding-removed`, `structural-edit`); enforce line cap (≤10 clean, 11–15 warning, >15 abort); insert at top of file (most-recent-first ordering invariant). **Item 2 — Retroactive stub backfill for the 11 staged guides.** One-time pass writes a stub entry `## 2026-04-19 — Session 44 — initial-synthesis` to each existing guide's companion file before any re-synthesis runs. Stub bullet shape: Findings count from each guide's current `source_findings[]`, no added/removed (initial), Structural = 'initial synthesis; no prior version', Preserved = 'none', SL = `[[session-44-codifier-extraction-run]]`. The `initial-synthesis` trigger tag is permitted ONLY for backfill stubs (not added to the live enum). Acceptance: 6 cases from DD-94 §Acceptance Criteria — companion file created when absent; new entry inserted at top when present; trigger-tag-not-in-enum aborts write; >15-line entry aborts write; 11–15 lines writes with warning; 11 staged guides each carry the initial-synthesis stub. Phase-1 lifecycle bundle (alongside IB-154, IB-156)."
milestone: null
---
