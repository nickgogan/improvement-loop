# Transcript: The INSANE Engineering Behind REPLIT Agents.

**URL:** https://www.youtube.com/watch?v=c8QiXuUMZCI
**Segments:** 743
**Channel:** Akhil Sharma
**Duration:** 23:49
**Uploaded:** 2026-06-13

---

## Full Text

In 2024, Replit shipped something that almost no other AI coding company had ever shipped before, which was an agent that doesn't just write code for you. It runs the code, tests the code, installs packages, spins up a database, and deploys the app. All of that end-to-end, and you as a developer just enter a single prompt, "Build me a calorie tracking web application." But Replit builds it all completely and gives you a publicly accessible URL. In today's video, we're going to break down how Replit does all of this. Now, this is a very visual video where I have a lot of diagrams for you. So, I want you to stick around till the end because what Replit did was solve a very interesting and challenging engineering problem. Now, the thing is, most AI coding tools at that time were doing it the obvious way, where the AI writes some code for you, and you copy it in your editor, and you run it on your machine. Whereas Replit went in the other direction. They decided that the AI agent should get its own full environment in the cloud. That means a real Linux box with a real shell, a real file system, a real database, and a real network. And every single user will get their own. So, in this case, the AI agent is not really an AI that's typing in your editor. It's actually an AI that's sitting at its own dedicated computer somewhere in Google's data centers doing the work that you would be doing on your own. And thousands of those dedicated computers get spun up every single day, and each one is isolated from every other one. And each one is fully recoverable if the agent breaks something. And each one completely disappears the moment you stop using it. Now, when you write a text like, "Build me a Twitter clone," and you pass it off to Replit as a prompt, there's actually enormous amount of infrastructure that has to work. And most of it is the kind of stuff that you would never know until you watch a video like this. And the reason the story matters is because this is one of the most interesting architectural problems of the next decade, which is how do you safely run untrusted code that an AI just wrote at massive scale while making the agent feel like it has a real computer. And the answer that Replit landed on and the implementation that they did as a single decision now cascades into every other piece of the architecture. And by the end of this video, you'll understand that decision that they made and you'll understand why it forced every other choice they also made after that. So, let's get into it. So, just to be clear, the central decision that Replit made is that the AI agent gets its own full development environment and not just a stripped-down sandbox. So, in the case of Replit, it would have a real shell, a real file system, a real package manager, a real Postgres database, a real deployment pipeline, and so on. Now, to understand why that's a big deal, you've got to understand the choice they were making against. If you're building an AI agent that needs to run code, you've basically got two options. Option one is the lightweight sandbox approach where you give the AI a constrained environment. It could be a Python interpreter inside a WebAssembly runtime or a single Docker container with a strict allow list of operations. That means it'll be fast to spin up, it'll be cheap, and it'll be safe because the AI can't do a lot in there. And most code execution APIs work this way. But OpenAI's code interpreter, Anthropic's code execution tool, and most of the lighter agentic systems, they're variants of this. The AI gets to run code but in a box that's been deliberately stripped down to what's safe. Now, option two is the full environment approach where you give the AI an actual Linux machine. It can install packages, it can run any shell command, it can read and write any file, it can hit the network, it can stand up its own database. It's not a sandbox in the lightweight sense at all. It's a complete development workstation, but it's just one that happens to live in the cloud and belongs to the AI for a session. Most AI tooling at the time was relying on option one because option one is way easier to make safe. The agent can't really hurt itself or anyone else because there's nothing in there to hurt. And the problem is that option one is also dramatically less capable. An AI that can't install packages cannot build modern software. An AI that cannot spin up a database cannot build a real application. And an AI that can't run a long-running server can't actually test the thing it just wrote. So, Replit looked at this and made the call that if you want the agent to actually build software and not just write snippets, it actually needs the full environment. And it would be the same environment that a human Replit developer would actually use because the unit of work is not generate some code. The unit of work here actually is ship a running app. And shipping a running app requires every tool that humans use. So, that was the decision. And every AI agent will get its own dedicated Replit workspace just like a human user would. So, the agent is the user in this case. And that's a huge structural shift. Now, the moment you make this decision, you've signed yourself up for a massive engineering project because now you have to figure out how to run thousands of full development environments with thousands of AI agents running arbitrary code inside them every single day without any of them losing data, especially when the agent inevitably does something wrong. Now, let's quantify what they were committing to. First, they needed isolation strong enough that an AI running arbitrary code in one environment cannot under any circumstances see or affect another user's environment. Now, this is a hard problem because the AI is going to do things that humans would not. It's going to run weird shell commands, install random packages, and even sometimes try to escape its environment. And it has to fail every single one of those times. Second, they needed the ability to spin up these environments cheaply and quickly. So, if a new user shows up and asks the agent to build something, they shouldn't wait 2 minutes for an environment to provision. Thousands of these get created and torn down every day. So, the life cycle has to be fast. Third, they needed complete reversibility. So, AI agents make mistakes, right? And they delete files, they can corrupt databases, they can install packages that break the build. And if a user cannot recover when that happens, the product is broken. So, whatever the agent does, the user has to be able to undo it cleanly and instantly. Fourth, they needed the deployed version of the app to be just as isolated as the development version. So, when the agent finishes building the Twitter clone and the user hits deploy, that thing is now serving real internet traffic. It has to be isolated from every other developed app on the platform. Otherwise, the first user with a malicious prompt takes the whole platform down. Fifth, they needed the agent itself to be reliable enough to actually use all of these capabilities productively. That means the agent's planning loop, its tool use, its error recovery, all of it has to be designed around the specific environment it's running in. The agent and the infra are not separate things. They are one system. So, with all that said, now let's actually look at how they built it. This four layers worth understanding, and each one is a consequence of that central decision, and each one is doing a lot of real work. The four layers, starting from the bottom in this diagram that you can see, the first one is the isolation layer, which is what keeps each environment safe from every other one. Then you have the snapshot layer, which is what makes the agent's actions reversible. Then the agent loop itself, which is what actually does the thinking and the tool use. And the deployment layer, which is what runs the finished app on the public internet. Each one has interesting decisions inside it. So, let's walk through them. So, first, the isolation layer. Now, this is the foundation, and everything else is built on top of it. Now, when you spin up a new Replit environment, what you actually get is a Linux container. Now, this Linux container is isolated using a combination of Linux kernel features and hypervisor-level boundaries. The simple version is every environment is its own little machine with its own file system, its own process tree, its own network name space, its own resource limits. And nothing inside one environment can see or touch anything on the one. But, the part that's interesting here is how strong that isolation has to be because the threat model here is different from a normal platform. In normal cloud platform, say Heroku in 2015, the assumption was that the customer running the workload is on average a reasonably well-behaved developer. They're not trying to break out of the container. They're just trying to ship their app. And the isolation has to be strong enough to keep accidents from spreading. But, for AI agents, that assumption is dead. The agent is going to do all kind of weird things. It's going to run commands that a developer would never run. It's going to install packages a developer would never install. And the user behind the agent might be deliberately trying to do something malicious. They might be prompting the agent to break out, to scan the host, to pivot to another customer's environment. And this is why the isolation has to assume hostile and not friendly. And this is why the lightweight sandbox approach we talked about earlier is so much easier because there's nothing in a stripped-down sandbox for an agent to exploit. But, Replit cannot make that simplifying assumption. They've got the full environment, as you can see in the diagram. So, they need stronger isolation than the typical container. The way they handle this is what they call defense in depth. At the development layer, where the agent is actively building, every environment is a sandbox container with strict resource limits and even namespace isolation and a hardened set of system call boundaries. So far, that's table stakes. The interesting part is what happens when you deploy. When an agent finishes building an app and hits deploy, Replit does not just promote that container into production. They actually put the deployed app in its own dedicated Google Cloud project. So, every single customer, including the free tier, gets their own GCP project for their deployments, which is a wild decision when you first see it because GCP projects are heavyweight. They have their own billing, own quota system. So, spinning up just another customer is expensive in operational terms. But, the reason they do this is because the GCP project boundary is the strongest tenant isolation primitive that Google Cloud offers. So, inside a single project, you have to trust the project itself. But, across projects, the isolation is enforced by Google's infrastructure, the same boundary that separates Google from its own customers. So, by giving every Replit customer their own project, Replit is essentially saying, "Even if everything else fails, even if a malicious agent breaks out of its container, the next thing it hits is Google's own tenant boundary, which has been beaten on by every adversarial researcher on the planet and has held up." Okay. So, we've got isolation and the agent has a safe place to do its thing now. But, the next problem is the one that actually defines whether an AI coding product is usable or not, which is what happens when the agent screws up. Now, before we go any deeper, I want to take a few seconds to tell you about a cohort that I teach personally to 15 to 20 senior engineers every few months. And it's called the AlgoRock cohort, where we cover advanced system design and architecture for the post-AI world. Usually, the people who attend these cohorts already know engineering and system design basics and want to build systems that take them to the next level. So, it's attended by platform engineers, principal engineers, SREs, enterprise and solution architects, and VP engineering. And there are people all the way from 5 years to 26 years of experience. And this is a great way to learn from actual production case studies. The next cohort starts in early July and I want you to go through this website called algorock.io, which is algorock.io, and you can check out the list of the top topics that we're going to cover. So, we'll cover modern systems like how Anthropic handles LLMs and how Hugging Face handles so many sandboxes for AI models, and how Together AI hosts so many open-source models at the same time. It's a 12-weeks intensive program, and if you like the topics, I want you to fill up the form that opens when you hit the reserve or enroll button. Once you've done that, I'll check to see if you're a good fit and we'll set up a call with you. The live cohort personally taught by me is about $2,500, and if you just wanted access to pre-recorded videos, that'll be about $800. Now, if you're early on in your career and beginning engineering right now, this cohort might not be very relevant to you, so please don't fill up the form. All right, now back to the video. Now, the thing is, AI agents are not very reliable. They look reliable when they work, but over a 30-step task, the probability of every step being right compounds into something pretty grim. The first few steps tend to be solid. By the time you're 20 steps in, the agent is making a lot of mistakes. It's already deleted a file it shouldn't have. It's modified the schema in a way that breaks the app. It's installed a conflicting dependency. It's done something that the user has no way to undo by hand. Now, if the user cannot recover from those moments, the product is fundamentally broken. So, it doesn't matter how good the agent is on the good runs. The bad runs poison the experience. The fix Replit built is called the snapshot engine, and it's quite impressive. The core idea is that every action the agent takes is fully reversible. So, every checkpoint, every change, all the way back to the start of the session. But, to pull that off, you have to think about state in three layers because the agent touches three different kinds of state, and they don't behave the same way. The first layer is the file system. This is where the code lives. So, files that the agent creates, files that it edits, files that it deletes. And the way Replit handles this is with copy-on-write underneath the file system. And the way copy-on-write works is that when you take a snapshot of a file system, you don't actually copy any data. You just record a marker that says, "This is where the file system was at this moment." So, future writes don't modify the original blocks. They write new blocks, and the new blocks become part of the live file system. And the original blocks stay around untouched, available to roll back to. Now, the beautiful part of copy-on-write is that the snapshot itself is constant time. So, it doesn't matter if your project is 1 MB or 1 GB, taking the snapshot is just writing a marker. And it just takes milliseconds. So, Replit can snapshot the agent's file system at every single step of its work without slowing anything down. And rolling back is just pointing the file system at an earlier marker. The second layer is the code itself. This sits on top of the file system, but it's a separate concern because the way you think about code changes, like diffs, commits, and branches, is different from how you think about file system state. Replit handles this with Git, but the way they use Git is interesting. Every meaningful action the agent makes automatically creates a Git commit in the background. So, even if the user never touches Git themselves, they're getting a complete commit history of everything the agent has done. And there is an immutable backup remote. So, even a full file system deletion does not lose the history. This means users can scroll back through the agent's work the same way that you scroll back to your own commits and revert to any point. So, for beginner users, it's just a single button that says, "Go back to before that broke." But for power users, it's a full Git panel where they can branch, merge, and cherry-pick. Now, after this, the third layer is the database, and this is where it gets generally hard because file systems and Git are both easy to snapshot. They're just bytes, but databases are different. A database has live connections, in-flight transactions, and internal write-ahead log. So, you can't just copy-on-write your way around a running Postgres. The way Replit handles this is something they call forkable Postgres. The development database, which is the one that the agent is using while it builds, is architected to support forking. So, when you take a snapshot, the database forks into a new branch that shares the data with the parent until something writes to it, which is the same copy-on-write idea applied to database state. So, you roll back and you just discard the fork branch. The really critical part of this is that the development database is completely separate from any production database at the architecture level. So, the agent is never allowed to touch production-level data ever. So, even if the agent fries its dev database six different ways, the data the user actually cares about is untouched. The dev database is meant to be disposable. And this is what reversibility actually means when you take it seriously. So, you have three layers of state, each handled with the right primitive for that layer, and the user gets one thing, which is undo the last thing the agent did. But underneath, that one operation is rolling back file system blocks, reverting git commits, and discarding a database fork. All of this in milliseconds, and all so that the user feels safe letting the agent try things. Now, here's the deeper thing about this design. Once you have this infrastructure, you can do something that completely changes what coding agents can do. You can run multiple agent attempts in parallel. Because if each attempt is a fork that's cheap to create and cheap to throw away, you can have the agent try three different approaches simultaneously, see which one breaks, and keep that one. And you can discard the other two. This is what people in the AI space call parallel sampling, which is exploiting the fact that the agent is non-deterministic to get a better answer by trying multiple times. Now, most AI coding products cannot do this because their state model cannot handle it, but Replit can because they built the snapshot engine first. Okay, so now we've got isolation, we've got reversibility, and now let's talk about the agent itself, which is the part that's actually doing all of the thinking. The Replit agent is built on what's called a React-style loop, which is now the standard pattern for tool-using agents. It's actually pretty simple at the conceptual level, where the agent has a goal, it reasons about what to do next, and then it calls a tool, and the tool gives back a result, and the agent uses that result to figure out the next thing to do. So, it's basically reason, act, observe, and repeat until the goal is done. What's specific to Replit is a set of tools the agent has access to, and that set is exactly the set of things a human Replit developer can do. The agent can write to the file system, it can run shell commands, it can install packages, it can create database tables, it can run servers and check if they work, it can read errors, it can deploy. So, basically the same toolbox that the human gets. Now, this is a meaningful design choice, and a lot of AI coding agents try to be clever about what tools the agent gets. They build specific primitives for the agent that humans don't use, or they try to abstract away the messy parts. Replit's bet is the exact opposite. You give the agents exactly the same surface a human has, because the agent has been trained on data where humans use shells and file systems and package managers. So, if you give the same tools to the agent, it knows how to use them. And if you give it any special tools, you're not training it from scratch on something it's never seen before. Now, there's another reason why this matters. By constraining the agent's environment to existing Replit tool set, Replit gets to lean on infrastructure that's already battle-tested. So, the package manager, the file system, the database, the deployment pipeline, all of these have been running for human users for years. They have known behaviors, known failure modes, and known edge cases. The agent gets all of that maturity for free. Compare that to building a custom agent infra from scratch, so you'd have to discover every edge case yourself in production with AI making them happen. And that's a brutal way to learn. So, the agent's loop is read the user's prompt, plan what to build, execute steps using the standard Replit tools, observe the results, and then adjust. Every action gets by the snapshot engine, and every action is reversible, and every meaningful step shows up in the UI as a clear message that the agent installed this package, the agent created this file, and the agent ran this command. The user can follow along and intervene if something looks wrong. Now, over time, that single agent loop has actually grown into a multi-agent architecture where different specialized agents handle different parts of the work, like planning, coding, debugging, deployment, but the core idea is the same. The agent runs in a real environment with real tools, and every action is reversible. Okay, so now the agent finishes building, and the user looks at the app and says, "It works." and hits deploy. But what actually happens after that? Now, this is the moment where the development environment and the production environment have to be different things, because in development, you want everything to be flexible. The agent can install whatever it needs, restart things, break things, fix them. Whereas in production, you want the exact opposite. You want it to be locked down, predictable, isolated, and scaled to handle real traffic. Replit's deployment infrastructure runs on Google Cloud Run with the strong tenant isolation we talked about earlier, which is each customer's deployed apps live in their own GCP project. Now, the deployed app gets a public URL and sits behind Google's DDoS protection and web application firewall and scales up and down based on actual traffic. The development environment where the agent did its work can be torn down while the deployment is live or kept around for further iteration. I want you to notice about this handoff is how clean it is. The agent operates in one world, the development environment, with full flexibility and full reversibility, and the user's actual app operates in a different world, which is the production environment, which is locked down and isolated. The boundary between these two worlds is where the AI's freedom ends and where the platform's guarantees begin. And this separation is what makes the whole product work. If the agent's mistakes could touch production, you'd never let it work autonomously because there's a hard line wherein the agent, if it makes a mess in dev, then dev gets rolled back and production never sees it, so you can't let the agent work at all. Now, let's look at what all this costs because as senior engineers, we don't really get to skip the tradeoffs. So, the full environment per agent decision is expensive in ways that don't show up in a feature list, but it costs operational complexity. So, running a separate GCP project per customer means you're managing the life cycle of a huge number of projects. So, your provisioning quota billing and cleanup, all of it has to be automated and reliable, and most companies couldn't run this. They would drown in operational overhead, but Replit had to build internal tooling specifically to make this manageable at scale. And this costs a lot of compute. So, spinning up thousands of full environments per day, each with their own file system and databases, is dramatically more expensive than running thousands of lightweight sandboxes. And lightweight sandboxes are essentially free at scale. So, full environments are not, and that cost has to be either absorbed in the product margin or passed to the user. This also costs latency on cold starts. A full environment takes longer to spin up than a step down one. Replit has worked hard to make this fast, but the floor is fundamentally higher than what lightweight competitor can do. The next is that it costs surface area for things going wrong. So, every tool the agent has is another way for the agent to fail. The snapshot engine has to be perfect or you've got a product where users lose data. And the isolation has to be perfect or you've got a security incident. And the agent's planning has to be good or it makes a mess in an environment where it could do real work. Every powerful capability is a place where bugs can become catastrophic. Now, for Replit specifically, this bet has paid off and the full environment approach is what makes the agent actually capable of building real software and not just generating snippets. So, the lesson here is that when you commit to a powerful capability, you're committing to paying the operational and engineering tax of what that capability can do forever. So, when you adopt powerful capabilities, you have to look at their trade-offs as well. Okay, so there were five lessons in this video. The first one was the agent and the environment have to be designed together. The second one was that the reversibility is a feature that makes autonomy possible. The third one was you can borrow somebody else's strongest isolation boundary, where Replit puts every customer in their own GCP project because Google's stand boundary is very stable and has been attacked by a lot of people, and that means it's very stable. Fourth is that you should give the agent the same surface that humans would use. Fifth is the cost of a powerful capability is actually paid every single day forever. And Replit's full environment per agent decision is brilliant, but it costs a lot operationally. Now, this is the kind of structure and deep thinking that we go down deep in the AlgoExpert cohort, which is 12 weeks intensive program for senior engineers only. We look at real systems and real trade-offs. And if this was useful to you, check out algoro.io. Okay, so let's wrap this up. Replit's AI coding agent works because they made one decision early where the agent gets a full development environment, which is the same that a human would get. And they built every layer of the stack underneath it to make that decision safe. So, they built container isolation, GCP project per customer, copy-on-write file systems, forkable databases, and automatic Git commits, and even react loops with standard tooling and a clean handoff between development and deployment. And there is a lesson here that's bigger than Replit, and the lesson is that when AI agents are doing real work in the cloud, the infra underneath them is doing far more than the agent itself. The agent is actually just a thin layer of intelligence sitting on top of a thick stack of isolation, reversibility, and operational machinery. So, when you're designing your next system that uses AI agents in it, don't start by just asking what model to use or what prompts to write, but try and understand the environment around the agent. Ask what happens when the agent screws up. Ask what isolation guarantees you need. All right, so making these deep dive videos is a lot of hard work, and making these diagrams is a lot of hard work as well. So, please don't forget to subscribe to this channel and like and share this video, and comment if you have any questions. So, I'll see you in the next video.
