# Transcript: Stop Using AI For This

**URL:** https://www.youtube.com/watch?v=OSJJYWaxkKQ
**Segments:** 196
**Channel:** Kyle Cook from Web Dev Simplified
**Duration:** 6:01
**Uploaded:** 2026-06-12

---

## Full Text

There is a massive problem right now with how people are using AI. Let's just take for example G stack from Gary Tan. These are a bunch of skills that people are utilizing inside of their development environment to give AI certain functionalities. If we take a look at some of these skills, for example, this one is for converting a markdown file into a PDF, you'll see that these skills are pretty much just code instructions written rather in code or in English to tell the AI what to do. So, you can see here, this is just a giant wall of code that it's saying, "Hey, if the user asked for a PDF, run this." And then here, we have more code to run if they asked for a PDF. Then finally, we have these kind of interesting things where it's like, here's an if statement written out in English that the AI should be able to read to tell the user what to do. So, depending on the output of this code, here's some English if statements to run to tell the AI how to understand what to happen, and then some more stuff. And if we even go further inside this section, you can see more of those human-readable if statements. It's just English if statements. And then if we go even further, there's a bunch of other stuff about code to run. And finally, at the bottom, we get even more code that should be run by the user down here for various different things. So, essentially, what this skill is doing is instead of telling the AI, "Hey, here's some certain certain things to know and to understand," it's instead a program written from top to bottom telling exactly what should happen. Here's code to run. If this, then do this. If this, then do this. Do you know what that sounds like? It sounds like a program to me. It sounds like code. It sounds like something that should run deterministically the exact same way every single time. And instead, what we're doing is we're taking something that is deterministic. We have written out deterministic code, and we've even written out deterministic instructions on what to do, and we're feeding it into a non-deterministic machine that can sometimes hallucinate or make mistakes or does not always do the same thing every time. Instead of generating a skill like this, it would be much smarter to generate a single command-line tool that you can then let your AI use, but that command-line tool will encompass all of this code, all of these if statements, and handle everything properly inside of a command line instead of handling it inside of some type of agent. Now, I know there are certain situations where it's better to let an agent determine what to run because you maybe have somewhat ambiguous things that you want this thing to do. But instead of giving all of the instructions in here for how to actually run and build a PDF, it'd be much better to create really small little files and things that can run, and then the AI just calls out to those different things for the user based on different things. So, instead of having this giant skill that has a bunch of if checks inside of it, instead you give tools to the AI and then the AI itself can just determine which tools to call based on what you said. And this isn't just a problem with these particular skills or skills in general. This is something that's more of a trend I'm seeing everywhere where developers are essentially using their AI to do deterministic things, but they are using a non-deterministic tool. All the time I see developers telling their AI, "Hey, get in get commit this and then push it to GitHub." or something along those lines where it'd be much easier just to use a command line tool directly to do those different things. Or they're asking their AI to do this really complicated, you know, thing with a bunch of different if checks and so on when in reality it'd be much better for you to just have your AI write a program that does that thing deterministically, and then you could just call that program instead of wasting tokens having AI redo this every single time. Because again, whenever you call these skills or use these skills, the AI must read all of that text which is costing you money on tokens, and then must output and think a bunch which is costing you money on tokens. So, it'd be much better to spend your time generating all of this code one time, and then call that code on your computer to do this particular thing. Think about it. We have learned that caching is an incredibly important thing to do as you want to scale out your application. If you cache your application, it'll be much quicker because instead of generating the HTML every time someone accesses your page, you generate it once and you give the same HTML every single time making it much quicker, and your server runs much less because it generates all the code one time, and then it continually serves that same thing to the user. We've essentially completely forgotten that when it comes to AI. Instead of creating a program which does what we want us to do, the caching portion, we're instead forcing the AI to burn tokens and energy and power every time we want to do something instead of writing a program one time that'll do it for us every single time that we want to do this. I have a video editor program that I created and that video editor program does have some AI related things that happen inside of it. But, I have essentially taken that AI portion and I broken it out entirely and the rest of my application is entirely deterministic. For example, I use an AI to create a transcript of all of my text. That way I can edit my videos based on the transcript of the text. I can then stitch together those clips using FFmpeg and I can do various other things with that as well. But, the key is I don't tell the AI, "Hey, give me the transcript and then stitch together my FFmpeg videos together." I instead say, "Generate the transcript and put it in a JSON file." That JSON file is then read by my running program written in JavaScript and it reads that transcript, renders that out as a screen. I then can edit my video. I can then send a program to FFmpeg through my application, no AI at all, and it renders out the video. So, I'm able to use the AI for one small portion and I'm able to then take the output of the AI and run it in the rest of my program so everything else works like I expect it to. This is how AI should be used for these type of deterministic tasks because if I instead told the AI, "Generate the transcript, cut the video, render the FFmpeg stuff." I guarantee you at some point along that chain it's going to make a single mistake and if it does, everything in that chain is essentially broken cuz now my output no longer works and I need to rerun the entire chain from the absolute beginning. Instead, if I do a deterministic step-by-step process, generate the transcripts and then let my program do everything else, if my transcripts fail, that's fine. I just rerun the transcripts. But, for example, everything else will still work perfectly fine because it's all inside of this deterministic package. So, I think using AI to write the code for these deterministic things, that's a great thing. Do that 100%. But, having the AI do essentially deterministic tasks, but by sending it English to do that, that is just a waste of money, a waste of tokens, a waste of time, and it's going to give you worse results than if you just had a program that did that exact same thing for you.
